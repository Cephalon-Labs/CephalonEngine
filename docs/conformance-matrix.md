# Cephalon conformance matrix

This document is the per-package adoption-truth matrix for every shipped `Cephalon.*` package. Operators, platform teams, and AI agents can read this page to compare packages along the same dimensions without having to cross-reference the maturity audit, the component catalog, and the runtime contract index by hand.

Cross-references: [`engine-surface-maturity-audit.md`](engine-surface-maturity-audit.md), [`runtime-contract-index.md`](runtime-contract-index.md), [`components/README.md`](components/README.md), [`architecture.md`](architecture.md), [`architecture-review-2026-05.md`](architecture-review-2026-05.md), [`engineering-standards.md`](engineering-standards.md), [`compatibility.md`](compatibility.md), [`project-memory.md`](project-memory.md).

## How to read this matrix

Each row describes one shipped Cephalon package along these dimensions:

- **Package** — the published `Cephalon.*` package name
- **Family** — the shipping family this package belongs to (12 families total, see below)
- **Maturity** — `M0`/`M1`/`M2`/`M3`/`M4` per [`engine-surface-maturity-audit.md`](engine-surface-maturity-audit.md)
- **Ownership mode** — `taxonomy-only` / `application-managed` / `cephalon-managed` / `provider-managed` (or a mixed combination)
- **Engine routes** — `/engine/*` HTTP routes the package contributes when active
- **Snapshot keys** — `snapshot.*` keys the package contributes to `RuntimeIntrospectionSnapshot`
- **Catalog interfaces** — `I*Catalog` / `I*RuntimeCatalog` interfaces the package owns
- **Notes** — one-line summary; "(audit pending)" flags rows where the maturity audit is currently silent or where component docs and audit doc disagree, while "(family-covered by maturity audit)" flags rows where the audit declares maturity through a consolidated family-level entry rather than per-package detail

Maturity recap (full definitions in the maturity audit):

- `M0` — taxonomy or descriptor surface only; no runtime claim
- `M1` — runtime catalog or descriptor truth wired in; no managed execution
- `M2` — narrow managed execution proof (single vertical slice or dispatch loop)
- `M3` — broader managed execution; multiple vertical paths working together
- `M4` — adoption-ready surface; consumers can rely on it across project shapes

Ownership recap:

- `taxonomy-only` — surface exists as a name and shape, no behavior claim
- `application-managed` — runtime ownership belongs to the consuming app
- `cephalon-managed` — runtime ownership belongs to the engine itself
- `provider-managed` — runtime ownership belongs to a specific provider companion pack

Family recap (12 families, in order below):

`core-runtime`, `host-adapter`, `transport-adapter`, `behaviors`, `eventing`, `agentics`, `data-and-cdc`, `event-sourcing`, `multi-tenancy`, `audit-and-identity`, `edge`, `observability`, `scaffolding-and-tooling`.

When this matrix conflicts with the maturity audit or a component doc, the audit is the authoritative truth for maturity and the component docs are the authoritative truth for ownership detail. This page is the consolidated read; it should not become a third source of truth.

## Core runtime

| Package | Maturity | Ownership | Engine routes | Snapshot keys | Catalog interfaces | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `Cephalon.Abstractions` | M4 | cephalon-managed | — | — | many (`IModule`, `IAppBehavior`, `IBehaviorContext`, `IAgentToolDispatcher`, `IKnowledgeIndexCatalog`, `IEventPublicationDispatcher`, `ITenantResolver`, ...) | host-agnostic contract layer; stable across every domain |
| `Cephalon.Engine` | M4 | cephalon-managed | `/manifest`, `/snapshot`, `/app-model`, `/capabilities`, `/modules`, `/packages`, `/technologies`, `/patterns` | `Manifest`, `Status`, `OperationalStory`, `DiagnosticsConventions` plus composition for every snapshot key | `ICellBoundaryCatalog`, `ICellRouteCatalog`, `ICellHealthIsolationCatalog`, `ICellTrafficAutomationRuntimeCatalog`, `ITechnologyRuntimeCatalog`, `IFeatureFlagRuntimeCatalog` | composition center, manifest, runtime introspection, policy evaluation; `EngineRuntime` is one of the seven M1 redaction emission sites — module-phase activity tags (`runtime.{phase}` and `module.{phase}` spans during initialize/start/stop) route through the registered `RedactionPipeline` before exporter dispatch (`ENG-366`) |
| `Cephalon.Engine.SourceGen` | M1 | cephalon-managed | — | — | — | compiler-only source generator for module discovery descriptors; keeps assembly/package/configuration discovery off runtime assembly scans |

The core-runtime family is adoption-ready and stable. Priorities are compatibility hygiene, docs alignment, and absorbing new companion packs without core surgery.

## Host adapters

| Package | Maturity | Ownership | Engine routes | Snapshot keys | Catalog interfaces | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `Cephalon.AspNetCore` | M4 | cephalon-managed | every base `/engine/*` plus `/health`, `/health/live`, `/health/ready`; projects every optional `/engine/*` route when its companion is active | projects every `snapshot.*` key over HTTP | every shipped catalog interface via projection | HTTP-first host adapter; the `/engine/*` projection layer; `HttpRequestResponseLoggingMiddleware` is one of the seven M1 redaction emission sites — HTTP request/response span tags route through the registered `RedactionPipeline` before exporter dispatch (`ENG-365`) |
| `Cephalon.Worker` | M4 | cephalon-managed | — | — | — | generic-host worker without HTTP; split-configuration support; `RuntimeHostedService` is one of the seven M1 redaction emission sites — `worker.lifecycle.start` and `worker.lifecycle.stop` span tags (`cephalon.lifecycle.phase` / `cephalon.blueprint` / `cephalon.module.count`) route through the registered `RedactionPipeline` before exporter dispatch (`ENG-412`) |

Both host adapters are adoption-ready. Web hosts go through `Cephalon.AspNetCore`; pure background or worker hosts go through `Cephalon.Worker`. Adapter responsibilities stay thin: the host adapter does not own runtime truth, only its projection.

## Transport adapters

| Package | Maturity | Ownership | Engine routes | Snapshot keys | Catalog interfaces | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `Cephalon.AspNetCore.GraphQL` | M2 | cephalon-managed | — (hooks into `Cephalon.AspNetCore`) | — | — | GraphQL transport route mapping companion |
| `Cephalon.AspNetCore.JsonRpc` | M2 | cephalon-managed | — (hooks into `Cephalon.AspNetCore`) | — | — | JSON-RPC transport route mapping companion |
| `Cephalon.AspNetCore.Grpc` | M2 | cephalon-managed | — (hooks into `Cephalon.AspNetCore`) | — | — | gRPC transport route mapping companion |

Transport adapters are narrow execution proofs. REST is base in `Cephalon.AspNetCore`; GraphQL, JSON-RPC, and gRPC ship as optional alternative media types over the same route ownership.

## Behaviors and execution

| Package | Maturity | Ownership | Engine routes | Snapshot keys | Catalog interfaces | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `Cephalon.Behaviors` | M4 | cephalon-managed | — | `ExecutionGraphs`, `HostedExecutions`, `BehaviorResiliencePolicies`, `DurableExecutions`, `DurableExecutionStates`, `SagaChoreographies`, `SagaChoreographyPublicationStates` | `IBehaviorCatalog`, `IBehaviorRegistry`, `ISagaChoreographyRuntimeCatalog`, `ISagaChoreographyPublicationRuntimeStateCatalog`, `IBehaviorResilienceRuntimeCatalog`, `IDurableExecutionRuntimeCatalog`, `IDurableExecutionRuntimeStateCatalog` | adaptive behavior topology (ABT) dispatch, topology resolution, compatibility validation |
| `Cephalon.Behaviors.Http` | M2 | mixed: `application-managed` profile/publication activation plus `cephalon-managed` materialization, governance, and runtime catalogs | — (publishes through `Cephalon.AspNetCore`) | — | — | REST profile metadata plus explicit module-owned public REST activation; Cephalon-managed materialization, governance, and runtime catalogs |
| `Cephalon.Behaviors.Messaging` | M1 | application-managed | — | — | — | messaging transport bindings, descriptors only |
| `Cephalon.Behaviors.Patterns` | M1 | application-managed | — | — | — | pattern composition helpers (saga publisher contracts, durable execution helpers) |
| `Cephalon.Behaviors.SourceGen` | M1 | cephalon-managed | — | — | — | source generator for behavior registration and topology helpers |
| `Cephalon.Resilience` | M2 | mixed: `application-managed` descriptors + `cephalon-managed` runtime | — | — | `IBehaviorResilienceExceptionClassifier` (consumer-implementable; default classifier ships in this package); `IBehaviorResilienceRuntimeCatalog` stays in `Cephalon.Behaviors` because the runtime catalog is behavior-coupled | engine-managed resilience runtime (Polly v8 + `Microsoft.Extensions.Resilience` policy resolver, circuit-breaker state registry, default exception classifier, execution-context keys) extracted from `Cephalon.Behaviors` (`ENG-390`) so consumers can adopt resilience without taking the full behavior dispatch runtime; 4 of 7 files moved (`BehaviorResiliencePolicyResolver`, `BehaviorCircuitBreakerStateRegistry`, `DefaultBehaviorResilienceExceptionClassifier`, `BehaviorResilienceExecutionContextKeys`); 3 files (`BehaviorResilienceExecutionMiddleware`, `BehaviorIdempotencyResolver`, `BehaviorResilienceRuntimeCatalog`) stayed in `Cephalon.Behaviors` because moving would have widened internal surface or created circular project references; promotion to M3 is gated on an explicit operator surface (catalog routes, snapshot keys) landing inside this package |

The behaviors family is the engine's core execution backbone. `Cephalon.Behaviors` is adoption-ready; HTTP and messaging companions own their narrower transport seams. Pattern and source-generator companions are catalog/utility surfaces. `Cephalon.Resilience` is the dedicated engine-managed resilience runtime separated from `Cephalon.Behaviors` so consumer apps can adopt the resilience surface without the full behavior dispatch runtime.

## Eventing

| Package | Maturity | Ownership | Engine routes | Snapshot keys | Catalog interfaces | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `Cephalon.Eventing` | M3 | mixed: application-managed + cephalon-managed | `/event-subscription-readiness`, `POST /engine/event-publications`, `/engine/event-publications/runtime*`, `/engine/event-dispatches/terminal-failures`, `POST /engine/event-dispatches/{outboxId}/commands/{operationId}`, `/engine/event-dispatch-remediation-commands*` | `TechnologySurfaces`, `EventDispatchRuntimes`, `EventDispatchStates`, `EventPublicationStates`, `EventSubscriptionExecutionReadiness` | `IEventSubscriptionExecutionBindingCatalog`, `IEventSubscriptionExecutionReadinessCatalog`, `IEventPublicationRuntimeCatalog`, `IEventDispatchRuntimeCatalog`, `IEventDispatchRemediationDispatcher`, `IEventDispatchRemediationRuntimeCatalog` | channel/subscription descriptors, in-process execution baseline, bounded retry plus process-local or `IInbox`-backed completed-execution idempotency, config-driven event-type-to-channel publication routing, bounded process-local scheduled publication acceptance, publication/dispatch state truth, `event-dispatch-remediations` command-ready posture for retry/skip/quarantine dispatch-store commands, bounded command-result history through `event-dispatch-remediation-commands`, and `eventing-superiority-profile` claim-status evidence against MassTransit/NServiceBus/Wolverine/MediatR benchmarks; broker dead-letter, broker topology materialization, provider partition ownership, and distributed scheduling remain not claimed; `InProcessEventPublisher` is one of the seven M1 redaction emission sites — `eventing.publication.dispatch` span tags route through the registered `RedactionPipeline` before exporter dispatch (`ENG-371`) |
| `Cephalon.Eventing.Wolverine` | M3 | provider-managed | (projected through parent eventing) | `EventDispatchStates`, `EventPublicationStates` | — | optional Wolverine-managed dispatch lane with terminal dispatch/subscription retry; `WolverineEventDispatchHostedService` is one of the seven M1 redaction emission sites — `wolverine.dispatch` span tags including `cephalon.tenant_id` / `cephalon.correlation_id` / `cephalon.message_id` route through the registered `RedactionPipeline` before exporter dispatch (`ENG-374`) |
| `Cephalon.Eventing.Behaviors` | M1 | application-managed | — | — | — | behavior-native eventing bridge; saga choreography handoff into outbox-backed eventing |

Eventing's core baseline owns the native package hint, in-process execution lane, publication routing, publication/dispatch seams, operator state surfaces, and the machine-readable superiority profile. Wolverine is the first optional provider-managed dispatch follow-through, not the default generated answer for `event-driven-integration`. The behaviors-native bridge is the explicit phase-12 seam between saga choreography authoring and outbox-backed publication.

## Agentics and retrieval

| Package | Maturity | Ownership | Engine routes | Snapshot keys | Catalog interfaces | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `Cephalon.Agentics` | M3 | mixed: application-managed + cephalon-managed | `/engine/agent-tools`, `POST /engine/agent-tools/{toolId}/runs`, `/engine/agent-tool-runs`, `/engine/agent-tool-runs/retry-pending`, `/engine/agent-tool-runs/idempotency-duplicates`, `/engine/agent-tool-runs/approval-required`, `/engine/agent-tool-runs/terminal-failures` | `AgentToolRuns` | `IAgentToolRunCatalog`, `IAgentToolDispatcher`, `IAgentToolExecutor`, `IAgentToolExecutionPolicy`, `IAgentToolExecutionObserver` | tool descriptors, managed dispatch loop, bounded retry/idempotency, operator action seams; `AgentToolDispatcher` is one of the seven M1 redaction emission sites — `agentics.tool.dispatch` span tags (dispatcher/tool/run/actor/correlation identifiers) route through the registered `RedactionPipeline` before exporter dispatch (`ENG-401`) |
| `Cephalon.Retrieval` | M3 | mixed: application-managed + cephalon-managed | `/engine/knowledge-indexes`, `POST /engine/knowledge-indexes/{collectionId}/queries`, `POST /engine/knowledge-indexes/{collectionId}/reindex` | `KnowledgeIndexes` | `IKnowledgeIndexCatalog` | lexical indexing, query execution, freshness state, background reindex scheduler; `KnowledgeIndexer` and `KnowledgeQueryEngine` are two of the seven M1 redaction emission sites — `retrieval.knowledge.index` and `retrieval.knowledge.query` span tags route through the registered `RedactionPipeline` before exporter dispatch (`ENG-402`) |

Both agentic surfaces are managed vertical proofs. Cephalon-managed dispatch and indexing already work; broader durable agent queues, memory persistence, and provider-specific orchestration remain future work.

## Data and CDC

| Package | Maturity | Ownership | Engine routes | Snapshot keys | Catalog interfaces | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `Cephalon.Data` | M3 | mixed: cephalon-managed + provider-managed | `/engine/cdc-captures`, `/engine/cdc-captures/runtime*`, `/engine/cdc-capture-runtimes`, `POST /engine/cdc-capture-runtimes/{executionRuntimeId}/reports`, `/engine/data-products`, `/engine/outboxes`, `/engine/inboxes`, `/engine/projections`, `/engine/databases`, `/engine/database-topology`, `/engine/database-roles`, `/engine/database-migrations`, `/engine/database-migration-playbook`, `/engine/technology-surfaces` | `CdcCaptures`, `CdcCaptureStates`, `CdcCaptureExecutionRuntimes`, `DataProducts`, `Outboxes`, `Inboxes`, `Projections`, `DatabaseRoles`, `DatabaseMigrations`, `DatabaseMigrationPlaybook`, `DatabaseTopology`, `TechnologySurfaces` | `ICdcCaptureCatalog`, `ICdcCaptureExecutionRuntimeCatalog`, `IOutboxCatalog`, `IInboxCatalog`, `IDataProductCatalog`, `IProjectionCatalog`, `IDatabaseRoleCatalog`, `IDatabaseMigrationCatalog`, `ITechnologyRuntimeCatalog` | CDC runtime state, shared execution substrate, provider CDC capture/runtime technology surfaces, outbox/inbox descriptors, data product descriptors, database topology |
| `Cephalon.Data.EntityFramework` | M2 | cephalon-managed | — (projects through `Cephalon.Data` route families and technology surfaces) | `DataProducts`, `Outboxes`, `Inboxes`, `Projections`, `DatabaseRoles`, `DatabaseMigrations`, `DatabaseMigrationPlaybook`, `DatabaseTopology`, `TechnologySurfaces` | `IDataProductCatalog`, `IOutboxCatalog`, `IInboxCatalog`, `IProjectionCatalog`, `IDatabaseRoleCatalog`, `IDatabaseMigrationCatalog`, `ITechnologyRuntimeCatalog` | DbContext-backed read/write stores, database role/migration runtime truth, topology/runtime surfaces, projections, outbox/inbox, entity event sourcing baseline |
| `Cephalon.Data.SqlServer` | M2 | provider-managed | — (CDC projects via `Cephalon.Data`) | `CdcCaptures`, `CdcCaptureStates`, `CdcCaptureExecutionRuntimes`, `TechnologySurfaces` | `ICdcCaptureCatalog`, `ICdcCaptureExecutionRuntimeCatalog`, `ITechnologyRuntimeCatalog` | SQL Server data + provider-native CDC capture/runtime provider; database role/migration truth comes from `Cephalon.Data.EntityFramework` over `Engine:Databases` |
| `Cephalon.Data.Postgres` | M2 | provider-managed | — (CDC projects via `Cephalon.Data`) | `CdcCaptures`, `CdcCaptureStates`, `CdcCaptureExecutionRuntimes`, `TechnologySurfaces` | `ICdcCaptureCatalog`, `ICdcCaptureExecutionRuntimeCatalog`, `ITechnologyRuntimeCatalog` | PostgreSQL data + provider-native logical-replication CDC provider; database role/migration truth comes from `Cephalon.Data.EntityFramework` over `Engine:Databases` |
| `Cephalon.Data.MySql` | M2 | provider-managed | — (CDC projects via `Cephalon.Data`) | `CdcCaptures`, `CdcCaptureStates`, `CdcCaptureExecutionRuntimes`, `TechnologySurfaces` | `ICdcCaptureCatalog`, `ICdcCaptureExecutionRuntimeCatalog`, `ITechnologyRuntimeCatalog` | MySQL data + provider-native binlog CDC provider with opt-in live CDC integration evidence through the SciSharp adapter; database role/migration truth comes from `Cephalon.Data.EntityFramework` over `Engine:Databases` |
| `Cephalon.Data.MySql.SciSharpReplication` | M0 | provider-managed | — | — | — | Optional SciSharp-backed MySQL binlog transport adapter with opt-in live binlog integration evidence; carries the current third-party non-public transport reflection posture |
| `Cephalon.Data.Oracle` | M2 | provider-managed | — (CDC projects via `Cephalon.Data`) | `CdcCaptures`, `CdcCaptureStates`, `CdcCaptureExecutionRuntimes`, `TechnologySurfaces` | `ICdcCaptureCatalog`, `ICdcCaptureExecutionRuntimeCatalog`, `ITechnologyRuntimeCatalog` | Oracle Database data + provider-native LogMiner CDC provider with opt-in live CDC integration evidence; database role/migration truth comes from `Cephalon.Data.EntityFramework` over `Engine:Databases` |
| `Cephalon.Data.MongoDB` | M2 | provider-managed | — (CDC/outbox/inbox project via `Cephalon.Data` and technology surfaces) | `CdcCaptures`, `CdcCaptureStates`, `CdcCaptureExecutionRuntimes`, `Outboxes`, `Inboxes`, `TechnologySurfaces` | `IDataProductCatalog`, `ICdcCaptureCatalog`, `ICdcCaptureExecutionRuntimeCatalog`, `IOutboxCatalog`, `IInboxCatalog`, `ITechnologyRuntimeCatalog` | MongoDB data + change-stream CDC provider plus provider outbox/inbox runtime surfaces |
| `Cephalon.Data.Redis` | M1 | provider-managed | — (outbox/inbox project via `Cephalon.Data` and technology surfaces) | `Outboxes`, `Inboxes`, `TechnologySurfaces` | `IOutboxCatalog`, `IInboxCatalog`, `ITechnologyRuntimeCatalog` | Redis provider outbox/inbox + runtime surface; opt-in live provider canary in `tests/Cephalon.Tests.ProviderIntegration` |
| `Cephalon.Data.Neo4j` | M1 | provider-managed | — (outbox/inbox project via `Cephalon.Data` and technology surfaces) | `Outboxes`, `Inboxes`, `TechnologySurfaces` | `IOutboxCatalog`, `IInboxCatalog`, `ITechnologyRuntimeCatalog` | Neo4j provider outbox/inbox + runtime surface (family-covered by maturity audit) |
| `Cephalon.Data.Cassandra` | M1 | provider-managed | — (outbox/inbox project via `Cephalon.Data` and technology surfaces) | `Outboxes`, `Inboxes`, `TechnologySurfaces` | `IOutboxCatalog`, `IInboxCatalog`, `ITechnologyRuntimeCatalog` | Cassandra provider outbox/inbox + runtime surface (family-covered by maturity audit) |
| `Cephalon.Data.ClickHouse` | M1 | provider-managed | — (outbox/inbox project via `Cephalon.Data` and technology surfaces) | `Outboxes`, `Inboxes`, `TechnologySurfaces` | `IOutboxCatalog`, `IInboxCatalog`, `ITechnologyRuntimeCatalog` | ClickHouse provider outbox/inbox + runtime surface (family-covered by maturity audit) |
| `Cephalon.Data.Elasticsearch` | M1 | provider-managed | — (outbox/inbox project via `Cephalon.Data` and technology surfaces) | `Outboxes`, `Inboxes`, `TechnologySurfaces` | `IOutboxCatalog`, `IInboxCatalog`, `ITechnologyRuntimeCatalog` | Elasticsearch provider outbox/inbox + runtime surface with sanitized inline URI capability metadata (family-covered by maturity audit) |
| `Cephalon.Data.OpenSearch` | M1 | provider-managed | — (outbox/inbox project via `Cephalon.Data` and technology surfaces) | `Outboxes`, `Inboxes`, `TechnologySurfaces` | `IOutboxCatalog`, `IInboxCatalog`, `ITechnologyRuntimeCatalog` | OpenSearch provider outbox/inbox + runtime surface with sanitized inline URI capability metadata (family-covered by maturity audit) |
| `Cephalon.Data.Qdrant` | M1 | provider-managed | — (outbox/inbox project via `Cephalon.Data` and technology surfaces) | `Outboxes`, `Inboxes`, `TechnologySurfaces` | `IOutboxCatalog`, `IInboxCatalog`, `ITechnologyRuntimeCatalog` | Qdrant provider outbox/inbox + runtime surface (family-covered by maturity audit) |
| `Cephalon.Data.Nats` | M1 | provider-managed | — (outbox/inbox project via `Cephalon.Data` and technology surfaces) | `Outboxes`, `Inboxes`, `TechnologySurfaces` | `IOutboxCatalog`, `IInboxCatalog`, `ITechnologyRuntimeCatalog` | NATS provider outbox/inbox + runtime surface with sanitized inline URI capability metadata (family-covered by maturity audit) |
| `Cephalon.Data.Debezium` | M1 | provider-managed | — (CDC projects via `Cephalon.Data`) | `CdcCaptures`, `CdcCaptureExecutionRuntimes` | `ICdcCaptureCatalog`, `ICdcCaptureExecutionRuntimeCatalog` | Debezium-managed external CDC sourcing/runtime reporting (family-covered by maturity audit) |

`Cephalon.Data` core ships as M3 with truthful cross-provider CDC truth. Relational providers (`SqlServer`, `Postgres`, `MySql`, `Oracle`) own schema/role/migration truth at M2 and contribute provider-native CDC where applicable. Non-relational store providers (`MongoDB`, `Redis`, `Neo4j`, `Cassandra`, `ClickHouse`, `Elasticsearch`, `OpenSearch`, `Qdrant`, `Nats`) now expose provider-managed outbox/inbox descriptors through `IOutboxCatalog`, `IInboxCatalog`, and `outbox-producers` / `inbox-stores` runtime technology surfaces when the event-driven technology is active. MongoDB has default-running disposable data-provider proof, Redis has opt-in data/event-sourcing proof, and Cassandra, ClickHouse, Elasticsearch, NATS, Neo4j, OpenSearch, and Qdrant have opt-in data-provider proof that can run against pre-provisioned services or disposable Testcontainers-backed runtimes; the store providers still stay M1 until a provider-owned managed read/write/projection lifecycle lands. MongoDB stays M2 because it also owns the provider-native change-stream runner. `Cephalon.Data.Debezium` stays M1 as external managed-connector CDC runtime truth until the pack owns connector orchestration.

## Event sourcing

| Package | Maturity | Ownership | Engine routes | Snapshot keys | Catalog interfaces | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `Cephalon.EventSourcing` | M1 | application-managed | — (projects `event-sourcing` through `/engine/technology-surfaces/{surfaceId}`) | — | `IEventStoreCatalog` | event-sourced aggregate contracts, event-type registry, merged event-store catalog, and sanitized runtime surface entries (family-covered by maturity audit) |
| `Cephalon.EventSourcing.EntityFramework` | M1 | provider-managed | — (projected through the `event-sourcing` surface) | — | `IEventStoreContributor` | Entity Framework append/read event store with sanitized provider catalog contribution (family-covered by maturity audit) |
| `Cephalon.EventSourcing.MongoDB` | M1 | provider-managed | — (projected through the `event-sourcing` surface) | — | `IEventStoreContributor` | MongoDB append/read event store with sanitized provider catalog contribution (family-covered by maturity audit) |
| `Cephalon.EventSourcing.Redis` | M1 | provider-managed | — (projected through the `event-sourcing` surface) | — | `IEventStoreContributor` | Redis append/read event store with sanitized provider catalog contribution; opt-in live provider canary in `tests/Cephalon.Tests.ProviderIntegration` |
| `Cephalon.EventSourcing.Neo4j` | M1 | provider-managed | — (projected through the `event-sourcing` surface) | — | `IEventStoreContributor` | Neo4j append/read event store with sanitized provider catalog contribution (family-covered by maturity audit) |
| `Cephalon.EventSourcing.Cassandra` | M1 | provider-managed | — (projected through the `event-sourcing` surface) | — | `IEventStoreContributor` | Cassandra append/read event store with sanitized provider catalog contribution (family-covered by maturity audit) |
| `Cephalon.EventSourcing.ClickHouse` | M1 | provider-managed | — (projected through the `event-sourcing` surface) | — | `IEventStoreContributor` | ClickHouse append/read event store with sanitized provider catalog contribution (family-covered by maturity audit) |
| `Cephalon.EventSourcing.Elasticsearch` | M1 | provider-managed | — (projected through the `event-sourcing` surface) | — | `IEventStoreContributor` | Elasticsearch append/read event store with sanitized provider catalog contribution (family-covered by maturity audit) |
| `Cephalon.EventSourcing.OpenSearch` | M1 | provider-managed | — (projected through the `event-sourcing` surface) | — | `IEventStoreContributor` | OpenSearch append/read event store with sanitized provider catalog contribution (family-covered by maturity audit) |
| `Cephalon.EventSourcing.Qdrant` | M1 | provider-managed | — (projected through the `event-sourcing` surface) | — | `IEventStoreContributor` | Qdrant append/read event store with sanitized provider catalog contribution (family-covered by maturity audit) |
| `Cephalon.EventSourcing.Nats` | M1 | provider-managed | — (projected through the `event-sourcing` surface) | — | `IEventStoreContributor` | NATS append/read event store with sanitized provider catalog contribution (family-covered by maturity audit) |

The event-sourcing family remains `M1` because Cephalon does not yet own projection rebuild orchestration, snapshot lifecycle, retention, or background replay workers. It is no longer provider-invisible: `Cephalon.EventSourcing` projects the merged `IEventStoreCatalog` through the `event-sourcing` runtime surface, and all ten provider packs contribute sanitized append/read store descriptors without exposing connection strings, passwords, URI user-info, or provider secret values. Per-pack promotion to `M2` still requires managed event-store execution proof beyond the current append/read + catalog/runtime-truth baseline.

## Multi-tenancy

| Package | Maturity | Ownership | Engine routes | Snapshot keys | Catalog interfaces | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `Cephalon.MultiTenancy` | M2 | cephalon-managed | — | `TenantMemberships`, `TenantInvitations`, `TenantDomainOwnership`, `TenantGovernanceActions`, `TenantAdministration` | `ITenantResolver`, `ITenantContextAccessor` | tenant resolution, ambient context, governance boundaries |
| `Cephalon.MultiTenancy.Governance` | M2 | mixed: cephalon-managed + provider-managed | — (governance surfaces project as `surfaceId` drill-downs under `/engine/technology-surfaces/{surfaceId}` — `tenant-memberships`, `tenant-invitations`, `tenant-domain-ownership`, `tenant-governance-actions`, `tenant-administration` — rather than top-level `/engine/*` routes) | `TenantMemberships`, `TenantInvitations`, `TenantInvitationDeliveryRuns`, `TenantDomainOwnership`, `TenantGovernanceActions`, `TenantAdministration` | `ITenantMembershipCatalog`, `ITenantInvitationCatalog`, `ITenantDomainOwnershipCatalog`, `ITenantGovernanceActionCatalog`, `ITenantAdministrationWorkflow`, plus 20+ governance interfaces | governance descriptors, stores, validators, delivery dispatch, workflow transitions |
| `Cephalon.MultiTenancy.Governance.AspNetCore` | M2 | cephalon-managed | `POST /engine/tenant-administration/commands`, `POST /engine/tenant-invitations/delivery-dispatches`, `POST /engine/tenant-invitations/delivery-status`, `GET /engine/tenant-invitations/delivery-status/observations` | `TenantInvitationDeliveryRuns`, `TenantAdministration` | — | ASP.NET Core tenant-admin commands, delivery dispatch + status callbacks, bounded delivery-status observation reads |
| `Cephalon.MultiTenancy.Governance.HttpDelivery` | M2 | provider-managed | — (projects `tenant-invitation-delivery-http` through `/engine/technology-surfaces/{surfaceId}`) | — | `ITenantInvitationDeliverySender` | HTTP webhook invitation sender with sanitized endpoint/retry/idempotency/signing runtime truth |
| `Cephalon.MultiTenancy.Governance.SmtpDelivery` | M2 | provider-managed | — (projects `tenant-invitation-delivery-smtp` through `/engine/technology-surfaces/{surfaceId}`) | — | `ITenantInvitationDeliverySender` | SMTP relay invitation sender with sanitized relay/channel/credential-flag runtime truth |
| `Cephalon.MultiTenancy.Governance.SendGridDelivery` | M2 | provider-managed | — (projects `tenant-invitation-delivery-sendgrid` through `/engine/technology-surfaces/{surfaceId}`) | — | `ITenantInvitationDeliverySender` | SendGrid Mail API invitation sender with sanitized API-key/configured-contract runtime truth |
| `Cephalon.MultiTenancy.Governance.SendGridDelivery.AspNetCore` | M2 | cephalon-managed | — (callback receiver) | — | — | SendGrid Event Webhook translation and verification |
| `Cephalon.MultiTenancy.Governance.MailgunDelivery` | M2 | provider-managed | — (projects `tenant-invitation-delivery-mailgun` through `/engine/technology-surfaces/{surfaceId}`) | — | `ITenantInvitationDeliverySender` | Mailgun Messages API invitation sender with sanitized domain/API-key/configured-contract runtime truth |
| `Cephalon.MultiTenancy.Governance.MailgunDelivery.AspNetCore` | M2 | cephalon-managed | — (callback receiver) | — | — | Mailgun webhook translation, HMAC verification, idempotency |
| `Cephalon.MultiTenancy.Governance.AmazonSesDelivery` | M2 | provider-managed | — (projects `tenant-invitation-delivery-amazon-ses` through `/engine/technology-surfaces/{surfaceId}`) | — | `ITenantInvitationDeliverySender` | Amazon SES v2 invitation sender with sanitized region/configuration-set/tag-key runtime truth |
| `Cephalon.MultiTenancy.Governance.AmazonSesDelivery.AspNetCore` | M2 | cephalon-managed | — (callback receiver) | — | — | Amazon SES SNS callback translation, verification, idempotency |
| `Cephalon.MultiTenancy.Governance.MicrosoftGraphDelivery` | M2 | provider-managed | — (projects `tenant-invitation-delivery-microsoft-graph` through `/engine/technology-surfaces/{surfaceId}`) | — | `ITenantInvitationDeliverySender` | Microsoft Graph sendMail invitation sender with sanitized Graph endpoint/mailbox-scope/token-configured runtime truth |
| `Cephalon.MultiTenancy.Governance.MicrosoftGraphDelivery.AzureIdentity` | M2 | provider-managed | — (projects `tenant-invitation-delivery-microsoft-graph-azure-identity` through `/engine/technology-surfaces/{surfaceId}`) | — | — | Azure.Identity token provider for Microsoft Graph with sanitized scope/authority/credential-exclusion runtime truth |

Multi-tenancy is one of the deepest companion proofs in the repo: 21 governance proofs landed between `ENG-235` and `ENG-256`, plus seven independent outbound sender/token-provider integrations. The maturity audit now carries a single delivery-sender family row that records the deliberate shared-`M2` floor across all ten sender / sender-companion packs (the six senders plus the four ASP.NET Core / Azure Identity companions). The seven outbound sender/token-provider packs now also project sanitized provider runtime surfaces so operators can see active provider wiring without exposing API keys, access tokens, passwords, webhook signing secrets, header values, or credential identifiers. Per-sender promotion above the family floor requires sender-specific adoption proof plus matching audit/matrix/component-doc updates in one slice. Distributed governance storage, identity-provider sync, and additional sender families remain future work.

## Audit and identity

| Package | Maturity | Ownership | Engine routes | Snapshot keys | Catalog interfaces | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `Cephalon.Audit` | M1 | mixed: `cephalon-managed` in-memory writer baseline + catalog projection + diagnostics; `application-managed` consumer-supplied actor accessors and durable storage opinion | `/engine/audit-stores`, `/engine/audit-history`, `/engine/audit-history/export` | `AuditStores` | `IAuditStoreCatalog` | host-agnostic audit-recording baseline; `/engine/audit-*` routes are M1 catalog projections of `IAuditStoreCatalog`/`IAuditHistoryReader`/`IAuditHistoryExporter` truth |
| `Cephalon.Audit.EntityFramework` | M1 | provider-managed | — | — | — | Entity Framework audit history: queryable `IAuditHistoryReader` plus bounded NDJSON `IAuditHistoryExporter` over `Engine:Audit:History` configuration |
| `Cephalon.Identity` | M1 | mixed: `cephalon-managed` default metadata-driven evaluator, runtime surface, catalog projection, diagnostics; `application-managed` identity scheme and principal flow | `/engine/authorization-policies` | `AuthorizationPolicies` | `IAuthorizationPolicyCatalog` | host-agnostic identity and authorization baseline; `/engine/authorization-policies` is the M1 catalog projection of `IAuthorizationPolicyCatalog` truth, and `identity-authorization` is the runtime surface |
| `Cephalon.Identity.AspNetCore` | M1 | application-managed | — | — | — | ASP.NET Core integration: minimal-API and controller policy mapping over the host-agnostic evaluator, optional `ClaimsPrincipal` projection into the ambient `IAuditActorAccessor` contract |
| `Cephalon.Ids.Sfid` | M2 | cephalon-managed | — | — | — | Snowflake ID (Sfid) generator with canonical sorting |

Audit and identity ship as host-agnostic baselines at M1: each pack contributes truthful catalog projections (`/engine/audit-*`, `/engine/authorization-policies`) plus narrow Cephalon-managed defaults (in-memory audit writer, metadata-driven authorization evaluator, identity runtime surface) without forcing one durable storage opinion or one identity scheme onto consumers. The Entity Framework audit pack is the first durable provider follow-through. Sfid stands alone at M2 as a fully managed identifier-generator surface. Distributed audit ledgers, broader authorization-decision proof, durable policy stores, and provider-specific identity-provider sync remain future work.

## Edge

| Package | Maturity | Ownership | Engine routes | Snapshot keys | Catalog interfaces | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `Cephalon.Edge` | M2 | cephalon-managed | `/engine/cells`, `/engine/cell-routes`, `/engine/cell-health-isolations`, `/engine/cell-traffic-automations` | `CellBoundaries`, `CellRoutes`, `CellHealthIsolations`, `CellTrafficAutomations` | `ICellTrafficAutomationRuntimeCatalog` (contributor) | edge node descriptors, cell traffic automation materialization seam |
| `Cephalon.Edge.KubernetesGateway` | M3 | provider-managed | — (projects via `Cephalon.Edge`) | `CellTrafficAutomations` | `ICellTrafficAutomationProviderMaterializer` | Kubernetes Gateway API materialization with apply-and-reconcile + cleanup sweep |
| `Cephalon.Edge.Traefik` | M3 | provider-managed | — (projects via `Cephalon.Edge`) | `CellTrafficAutomations` | `ICellTrafficAutomationProviderMaterializer` | Traefik IngressRoute materialization with apply-and-reconcile + cleanup sweep |

Edge already has two real provider-native control-plane materializers running on a shared lifecycle vocabulary (ownership, dependency, drift, lifecycle action, condition). Additional edge providers (Istio, Envoy, AWS App Mesh, etc.) remain future work.

## Observability

| Package | Maturity | Ownership | Engine routes | Snapshot keys | Catalog interfaces | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `Cephalon.Observability` | M2 | cephalon-managed | (startup summary projects via `/manifest`); `/engine/technology-surfaces` | `Manifest` (startup summary); `TechnologySurfaces` | `ITechnologyRuntimeContributor` | observability options, startup summaries, shared telemetry contract, sanitized `telemetry-export-guidance` runtime truth |
| `Cephalon.Observability.OpenTelemetry` | M1 | cephalon-managed | `/engine/technology-surfaces` | `TechnologySurfaces` | `ITechnologyRuntimeContributor` | OTLP exporter configuration plus sanitized `telemetry-export-opentelemetry` runtime truth (family-covered by maturity audit) |
| `Cephalon.Observability.Kubernetes` | M1 | cephalon-managed | `/engine/technology-surfaces` | `TechnologySurfaces` | `ITechnologyRuntimeContributor` | Kubernetes collector configuration plus sanitized `telemetry-export-kubernetes` runtime truth (family-covered by maturity audit) |
| `Cephalon.Observability.Aws` | M1 | cephalon-managed | `/engine/technology-surfaces` | `TechnologySurfaces` | `ITechnologyRuntimeContributor` | AWS X-Ray and CloudWatch configuration plus sanitized `telemetry-export-aws` runtime truth (family-covered by maturity audit) |
| `Cephalon.Observability.Gcp` | M1 | cephalon-managed | `/engine/technology-surfaces` | `TechnologySurfaces` | `ITechnologyRuntimeContributor` | GCP Cloud Trace and Monitoring configuration plus sanitized `telemetry-export-gcp` runtime truth (family-covered by maturity audit) |
| `Cephalon.Observability.AzureMonitor` | M1 | cephalon-managed | `/engine/technology-surfaces` | `TechnologySurfaces` | `ITechnologyRuntimeContributor` | Azure Monitor configuration plus sanitized `telemetry-export-azure-monitor` runtime truth (family-covered by maturity audit) |
| `Cephalon.Observability.NewRelic` | M1 | cephalon-managed | `/engine/technology-surfaces` | `TechnologySurfaces` | `ITechnologyRuntimeContributor` | New Relic OTLP configuration plus sanitized `telemetry-export-new-relic` runtime truth (family-covered by maturity audit) |
| `Cephalon.Observability.GrafanaCloud` | M1 | cephalon-managed | `/engine/technology-surfaces` | `TechnologySurfaces` | `ITechnologyRuntimeContributor` | Grafana Cloud configuration plus sanitized `telemetry-export-grafana-cloud` runtime truth (family-covered by maturity audit) |
| `Cephalon.Observability.AlibabaCloud` | M1 | cephalon-managed | `/engine/technology-surfaces` | `TechnologySurfaces` | `ITechnologyRuntimeContributor` | Alibaba Cloud configuration plus sanitized `telemetry-export-alibaba-cloud` runtime truth (family-covered by maturity audit) |
| `Cephalon.Observability.OracleCloud` | M1 | cephalon-managed | `/engine/technology-surfaces` | `TechnologySurfaces` | `ITechnologyRuntimeContributor` | Oracle Cloud APM configuration plus sanitized `telemetry-export-oracle-cloud` runtime truth (family-covered by maturity audit) |
| `Cephalon.Observability.DigitalOcean` | M1 | cephalon-managed | `/engine/technology-surfaces` | `TechnologySurfaces` | `ITechnologyRuntimeContributor` | DigitalOcean App Platform configuration plus sanitized `telemetry-export-digitalocean` runtime truth (family-covered by maturity audit) |
| `Cephalon.Observability.HuaweiCloud` | M1 | cephalon-managed | `/engine/technology-surfaces` | `TechnologySurfaces` | `ITechnologyRuntimeContributor` | Huawei Cloud APM configuration plus sanitized `telemetry-export-huawei-cloud` runtime truth (family-covered by maturity audit) |
| `Cephalon.Observability.OpenShift` | M1 | cephalon-managed | `/engine/technology-surfaces` | `TechnologySurfaces` | `ITechnologyRuntimeContributor` | OpenShift in-cluster collector configuration plus sanitized `telemetry-export-openshift` runtime truth (family-covered by maturity audit) |
| `Cephalon.Observability.Tanzu` | M1 | cephalon-managed | `/engine/technology-surfaces` | `TechnologySurfaces` | `ITechnologyRuntimeContributor` | VMware Tanzu configuration plus sanitized `telemetry-export-tanzu` runtime truth (family-covered by maturity audit) |
| `Cephalon.Observability.Serilog` | M1 | cephalon-managed | `/engine/technology-surfaces` | `TechnologySurfaces` | `ITechnologyRuntimeContributor` | Serilog provider integration plus sanitized `logging-provider-serilog` runtime truth (family-covered by maturity audit) |
| `Cephalon.Observability.DependencyHealth.Core` | M1 | cephalon-managed | — | — | — | dependency-health probe infrastructure (family-covered by maturity audit) |
| `Cephalon.Observability.*Dependencies` (Cassandra / ClickHouse / Consul / Elasticsearch / Http / Kafka / Memcached / MongoDB / MQTT / MySQL / NATS / Neo4j / OpenSearch / Oracle / PostgreSQL / RabbitMQ / Redis / SqlServer) | M2 | provider-managed | `/engine/dependencies` | — | `IDependencyHealthContributor` | provider-specific managed dependency-health probes with cached runtime truth, readiness impact for required failures, timeout/failure posture, and diagnostics event ids |

The observability family is broad, but it no longer treats dependency-health providers as taxonomy-only. The maturity audit now carries a single observability family row that confirms `Cephalon.Observability` core at `M2` `cephalon-managed`, the fourteen cloud / exporter configuration packs (OpenTelemetry, Serilog, Kubernetes, AWS, GCP, AzureMonitor, AlibabaCloud, HuaweiCloud, OracleCloud, DigitalOcean, OpenShift, Tanzu, GrafanaCloud, NewRelic) plus `Cephalon.Observability.DependencyHealth.Core` at `M1` `cephalon-managed`, and the eighteen per-provider dependency-health probe packs at `M2` `provider-managed`. The configuration packs are now runtime-visible through sanitized observability technology surfaces, but genuine telemetry collection still happens in the host runtime's OpenTelemetry or logging provider stack; promote a configuration pack above `M1` only when it owns runtime collection or a managed exporter loop, and promote a dependency-health probe pack above `M2` only when it adds operator automation, live reconciliation, or adoption evidence beyond the current managed probe loop.

## Scaffolding and tooling

| Package | Maturity | Ownership | Engine routes | Snapshot keys | Catalog interfaces | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `Cephalon.Scaffolding` | M4 | cephalon-managed | — | — | — | scaffold generation, package version catalog, deployment asset rendering |
| `Cephalon.Cli` | M4 | cephalon-managed | — | — | — | blueprint-driven app generation, package staging, doctor checks, docs publishing |
| `Cephalon.ReferenceDocs` | M2 | cephalon-managed | — | — | — | XML-doc reference publishing, hosted docs configuration |

The tooling family ships adoption-ready CLI plus scaffolding and an XML-doc reference publisher. Reference-doc generation runs against the live engine surface and is wired into release validation.

## Diagnostics and analyzer baseline

| Package | Maturity | Ownership | Engine routes | Snapshot keys | Catalog interfaces | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `Cephalon.Diagnostics` | M4 | cephalon-managed | `/engine/diagnostics-conventions` | — | `IRedactionFilter` (consumer-registered) | engine-level OpenTelemetry semantic-convention adapter; seven canonical `ActivitySource` / `Meter` names (`Engine` / `AspNetCore` / `Worker` / `Eventing` / `MultiTenancyGovernance` / `Agentics` / `Retrieval`) + `cephalon.*` attribute keys consumed by engine + both host adapters + `Cephalon.Observability.OpenTelemetry` (which subscribes to all seven canonical sources / meters as the OTLP companion pack's real subscription contract); `/engine/diagnostics-conventions` projects the canonical name set as a `DiagnosticsConventionsSurface` record; ships the `IRedactionFilter` + `RedactionContext` + `RedactionPipeline` redaction surface with `KeyMatchRedactionFilter` / `RegexRedactionFilter` starters and `IServiceCollection.AddRedactionPipeline()`; M1 wired at eight real engine emission sites (AspNetCore HTTP middleware, engine runtime module-phase tags, Wolverine dispatch tags, Cephalon.Agentics tool-dispatch tags, Cephalon.Retrieval knowledge-indexing + knowledge-query tags, Cephalon.Worker lifecycle-start + lifecycle-stop tags, Cephalon.Eventing publication-dispatch tags, and Cephalon.MultiTenancy.Governance invitation-delivery dispatch tags); per-package `EventId` ranges centralised in [`docs/diagnostic-id-registry.md`](diagnostic-id-registry.md) |
| `Cephalon.Analyzers` | M1 | cephalon-managed | — | — | — | curated consumer-facing analyzer meta-package bundling `BannedApiAnalyzers`, `PublicApiAnalyzers`, `Roslynator.Analyzers`, `Meziantou.Analyzer`, and `Microsoft.VisualStudio.Threading.Analyzers`; ships curated `BannedSymbols.txt` (auto-wired through `buildTransitive/Cephalon.Analyzers.props`) and `cephalon-analyzers.editorconfig`; `Cephalon.Abstractions` consumes the meta-package as the proof-of-concept |

The diagnostics + analyzer family ships the engine's canonical telemetry name set and the consumer-facing analyzer baseline. Both packages began as `M0` taxonomy-only and were promoted to `M1` once `Cephalon.Engine` (for diagnostics) and `Cephalon.Abstractions` (for analyzers) wired in the meta-package surface; promotion to `M2` is gated on the host-adapter rollout (`Cephalon.AspNetCore` / `Cephalon.Worker` for diagnostics) and the documented adoption path in `getting-started.md` plus template-pack starter defaults (for analyzers).

## Family summary at a glance

- **Adoption-ready (M4):** core runtime, host adapters, behaviors core, scaffolding, CLI, **`Cephalon.Diagnostics`** (canonical name set consumed by engine + both host adapters + `Cephalon.Observability.OpenTelemetry` as a real subscription contract)
- **Broad managed execution (M3):** eventing core, eventing Wolverine bridge, agentics, retrieval, data core, edge providers (Kubernetes Gateway, Traefik)
- **Narrow managed execution (M2):** transport adapters, behaviors HTTP, **`Cephalon.Resilience`** (engine-managed resilience runtime extracted from `Cephalon.Behaviors` via `ENG-390`), EntityFramework data, relational data providers (SqlServer, Postgres, MySql, Oracle, MongoDB), multi-tenancy core + governance + delivery senders, edge core, observability core plus the eighteen `Cephalon.Observability.*Dependencies` provider-managed probe packs, Sfid, ReferenceDocs
- **Catalog-only (M1):** behaviors messaging/patterns/sourcegen, eventing behaviors bridge, audit, identity, observability provider configuration packs, **`Cephalon.Analyzers`** (curated consumer-facing analyzer meta-package)
- **Provider-visible data catalog/runtime truth (M1/M2):** non-relational data store providers now project provider outbox/inbox descriptors through `IOutboxCatalog`, `IInboxCatalog`, and `outbox-producers` / `inbox-stores`; Debezium projects external managed-connector CDC through the shared CDC runtime surfaces, while provider-managed execution/provisioning remains future work for promotion beyond the current rows
- **Provider-visible append/read catalog/runtime truth (M1):** event-sourcing family (core plus ten provider event-store packs) now projects sanitized provider descriptors through `IEventStoreCatalog` and the `event-sourcing` runtime surface while managed projection/rebuild/snapshot/retention/replay lifecycle remains future work
- **M0 support-boundary packages:** `Cephalon.Data.MySql.SciSharpReplication` remains intentionally bounded by its current third-party non-public transport posture; no shipped `taxonomy-only` observability provider family remains after the dependency-health probe promotion.

## Inconsistencies observed (potential next ENG-* cards)

No open inconsistencies remain at this snapshot. The list below records the alignment items resolved between ENG-388 and ENG-419 so the durable history stays visible.

### Resolved alignment items (kept as durable history)

- **Relational data provider CDC/runtime ownership** (resolved May 2026): the SQL Server, PostgreSQL, MySQL, and Oracle provider rows used to overstate raw provider ownership of `DatabaseRoles`, `DatabaseMigrations`, `IDatabaseRoleCatalog`, and `IDatabaseMigrationCatalog`. Source truth keeps database role/migration ownership in `Cephalon.Data.EntityFramework` over `Engine:Databases`, while the raw relational provider packs own provider-native CDC descriptors and execution runtimes through the shared `Cephalon.Data` catalogs. The matrix now records those rows as `CdcCaptures`, `CdcCaptureStates`, `CdcCaptureExecutionRuntimes`, and `TechnologySurfaces`, adds `ITechnologyRuntimeCatalog` to the shared data/EF rows, and the composition suite now proves all four relational providers can be active together with an aggregated `data.relational-store` capability plus sanitized `data-management` CDC technology surfaces.
- **Observability dependency-health provider runtime promotion** (resolved May 2026): the source and hosting tests already proved concrete managed probe loops for all eighteen `Cephalon.Observability.*Dependencies` packs (Cassandra, ClickHouse, Consul, Elasticsearch, HTTP, Kafka, Memcached, MongoDB, MQTT, MySQL, NATS, Neo4j, OpenSearch, Oracle, Postgres, RabbitMQ, Redis, SQL Server), but the audit, matrix, and component badges still described them as `M0` `taxonomy-only`. The current matrix promotes the family row to `M2` `provider-managed`, records `/engine/dependencies` plus `IDependencyHealthContributor` as the runtime projection, and keeps further promotion gated on operator automation or adoption evidence beyond the managed probe baseline.
- **`Cephalon.Resilience` Family-summary M2 line entry** (resolved May 2026 via `ENG-419`): the *Family summary at a glance* section's *Narrow managed execution (M2)* line listed transport adapters, behaviors HTTP, EntityFramework data, etc. but missed `Cephalon.Resilience` even though the per-package row right above declared `M2` and `ENG-417` had added the row. The line now names `Cephalon.Resilience` as "engine-managed resilience runtime extracted from `Cephalon.Behaviors` via `ENG-390`" so the Family-summary-at-a-glance bullet matches the per-package row truth.
- **M1 redaction emission-site annotations on per-package rows** (resolved May 2026 via `ENG-418`, extended by later emission slices): the `Cephalon.Diagnostics` row in the matrix now states "M1 wired at eight real engine emission sites" and the per-package rows for the eight emission-source packages (`Cephalon.AspNetCore`, `Cephalon.Engine`, `Cephalon.Worker`, `Cephalon.Eventing.Wolverine`, `Cephalon.Agentics`, `Cephalon.Retrieval`, `Cephalon.Eventing`, `Cephalon.MultiTenancy.Governance`) name which class is the M1 emission site and that span tags route through the `RedactionPipeline` before exporter dispatch. Each per-package row names its emission class (`HttpRequestResponseLoggingMiddleware` / `EngineRuntime` / `RuntimeHostedService` / `WolverineEventDispatchHostedService` / `AgentToolDispatcher` / `KnowledgeIndexer` + `KnowledgeQueryEngine` / `InProcessEventPublisher` / `TenantInvitationDeliveryDispatcher`) and the closing-slice ENG reference (`ENG-365` / `ENG-366` / `ENG-412` / `ENG-374` / `ENG-401` / `ENG-402` / `ENG-371` / `ENG-379`) so a security reviewer scanning the matrix sees which packages are protected at the engine boundary without cross-referencing the Diagnostics row.
- **`Cephalon.Resilience` matrix row** (resolved May 2026 via `ENG-417`): `ENG-390` shipped the `Cephalon.Resilience` package extraction from `Cephalon.Behaviors` but the matrix per-package adoption-truth table didn't gain a row. The new row sits in the Behaviors-family section at `M2` `mixed: application-managed descriptors + cephalon-managed runtime` with `IBehaviorResilienceExceptionClassifier` (consumer-implementable; default classifier ships in this package) as the catalog interface and a Notes column that explains the 4-of-7 file move plus 3 kept files plus the M3 promotion gate. The Behaviors-family blurb is extended to name `Cephalon.Resilience` as the dedicated engine-managed resilience runtime separated from `Cephalon.Behaviors`.
- **`Cephalon.MultiTenancy.Governance` route-projection drift** (resolved May 2026 via `ENG-416`): `ENG-411` corrected `docs/runtime-contract-index.md` to declare that the five tenant governance surfaces project as `surfaceId` drill-downs under `/engine/technology-surfaces/{surfaceId}` rather than as top-level `/engine/*` routes, but the conformance-matrix row at line 146 still listed them as standalone top-level routes. The row now carries a clarifying note that the five surfaces project as `surfaceId` drill-downs, and the `Cephalon.MultiTenancy.Governance.AspNetCore` row's Engine routes column is extended from one route (`POST /engine/tenant-administration/commands`) to four (add `POST /engine/tenant-invitations/delivery-dispatches`, `POST /engine/tenant-invitations/delivery-status`, `GET /engine/tenant-invitations/delivery-status/observations`) so it matches the AspNetCore companion's actual route surface.
- **Data non-relational provider audit coverage** (resolved May 2026 via `ENG-393`, hardened May 2026 provider-runtime slice): the maturity audit now carries a family-level row for the non-relational `Cephalon.Data.*` store packs (`Redis`, `Neo4j`, `Cassandra`, `ClickHouse`, `Elasticsearch`, `OpenSearch`, `Qdrant`, `Nats`) at `M1` `provider-managed` provider-visible outbox/inbox + runtime-truth, while `Cephalon.Data.MongoDB` remains a separate `M2` row because it also owns the provider-native change-stream runner. Those store packs now contribute `IOutboxCatalog`, `IInboxCatalog`, and `ITechnologyRuntimeCatalog` truth, and the URI-based providers redact inline user-info/query credentials in manifest capability metadata. `Cephalon.Data.Debezium` remains `M1` external managed-connector CDC runtime truth until it owns connector orchestration. With this slice landed, the conformance matrix carries no remaining "(audit pending)" Notes-field tags and no longer describes these store providers as catalog-only.
- **`Cephalon.Behaviors.Http` REST publication ownership** (resolved May 2026 via `ENG-392`): the matrix row now records the explicit `application-managed` profile/publication activation plus `cephalon-managed` materialization, governance, and runtime catalogs split, mirroring the maturity audit row verbatim. The Notes column drops the "(audit pending — see inconsistency #1)" placeholder and now reads as a concrete summary of what each ownership half is responsible for. The shared component doc remains the per-package detail source.
- **Observability provider audit closure** (resolved May 2026 via `ENG-391`, superseded for dependency-health providers by the later M2 promotion): the maturity audit gained a consolidated observability family row that confirmed `Cephalon.Observability` core at `M2`, the cloud / exporter configuration packs plus `Cephalon.Observability.DependencyHealth.Core` at `M1`, and the per-provider dependency-health packs at the then-current `M0` taxonomy-only floor. The current provider-runtime promotion preserves the consolidated row shape but updates the per-provider dependency-health packs to `M2` `provider-managed` now that their concrete probe loops and hosting tests are part of the repo truth.
- **Multi-tenancy delivery sender maturity granularity** (resolved May 2026 via `ENG-390`): the maturity audit now carries a consolidated invitation-delivery sender family row that captures the deliberate shared-`M2` stance across `Cephalon.MultiTenancy.Governance.HttpDelivery`, `.SmtpDelivery`, `.SendGridDelivery`, `.MailgunDelivery`, `.AmazonSesDelivery`, `.MicrosoftGraphDelivery` plus the four `.SendGridDelivery.AspNetCore`, `.MailgunDelivery.AspNetCore`, `.AmazonSesDelivery.AspNetCore`, and `.MicrosoftGraphDelivery.AzureIdentity` companions. The family floor stays `M2` because each sender owns the same managed dispatch, callback handling shape (where applicable), replay protection, and idempotency hygiene over a different upstream API. Per-sender promotion above the floor requires sender-specific adoption proof unique to one sender (deeper reconciler, distributed retry queue, identity-provider sync, etc.) plus matching audit/matrix/component-doc updates in one slice; the matrix multi-tenancy family blurb now points at that audit row instead of leaving the shared-floor stance implicit.
- **Event-sourcing family audit coverage** (resolved May 2026 via `ENG-388`, hardened May 2026 provider-runtime slice): the maturity audit now carries a consolidated family-level row that confirms `Cephalon.EventSourcing` core plus the ten provider packs (`Cephalon.EventSourcing.EntityFramework`, `Cephalon.EventSourcing.MongoDB`, `Cephalon.EventSourcing.Redis`, `Cephalon.EventSourcing.Neo4j`, `Cephalon.EventSourcing.Cassandra`, `Cephalon.EventSourcing.ClickHouse`, `Cephalon.EventSourcing.Elasticsearch`, `Cephalon.EventSourcing.OpenSearch`, `Cephalon.EventSourcing.Qdrant`, `Cephalon.EventSourcing.Nats`) share an `M1` provider-visible append/read + catalog/runtime-truth stance with `application-managed` aggregate logic plus `provider-managed` per-pack store contracts. All ten providers now contribute sanitized descriptors into `IEventStoreCatalog` and the `event-sourcing` runtime surface; per-pack promotion to `M2` remains gated on managed projection/rebuild/snapshot/retention/replay lifecycle proof per provider pack.
- **Redis live provider canary** (resolved May 2026 via `ENG-501`): `tests/Cephalon.Tests.ProviderIntegration` now carries an opt-in Redis live-provider lane that proves `Cephalon.Data.Redis` outbox/inbox plus `Cephalon.EventSourcing.Redis` append/read behavior against one real Redis runtime while preserving the default Docker-free test posture. This strengthens reliability and data-integrity evidence without promoting either Redis pack beyond the current `M1` provider-visible baseline, because managed projection/rebuild/snapshot/retention/replay lifecycle remains a later provider-owned claim.
- **Non-relational data-provider live-proof wave** (resolved May 2026 via `ENG-512` / `ENG-513`): `tests/Cephalon.Tests.ProviderIntegration` now carries opt-in live-provider lanes for Cassandra, ClickHouse, Elasticsearch, NATS, Neo4j, OpenSearch, and Qdrant. The lanes prove real outbox/inbox writes, runtime manifest capabilities, and `event-driven-integration` surfaces, plus dispatch-store pending/success transitions for every provider except ClickHouse, where the explicit unsupported dispatch boundary is the contract. Each lane can now use either a pre-provisioned provider service or a disposable Testcontainers-backed provider runtime. This promotes scorecard evidence without promoting these providers beyond the current `M1` provider-visible baseline because managed read/write/projection lifecycle remains a later claim.
- **`Cephalon.Audit` and `Cephalon.Identity` route projection** (resolved May 2026 via `ENG-389`): the maturity audit now carries explicit per-package rows for `Cephalon.Audit` (`M1`, mixed `cephalon-managed` in-memory writer baseline plus catalog projection plus diagnostics with `application-managed` consumer-supplied actor accessors and durable storage), `Cephalon.Audit.EntityFramework` (`M1`, `provider-managed` durable Entity Framework audit history), `Cephalon.Identity` (`M1`, mixed `cephalon-managed` default evaluator/runtime surface/catalog projection/diagnostics with `application-managed` identity scheme and principal flow), and `Cephalon.Identity.AspNetCore` (`M1`, `application-managed` ASP.NET Core integration). The matrix Notes for those four rows now describe each route as an `M1` catalog projection of the matching catalog interface, with `identity-authorization` named explicitly as the runtime surface. The `Cephalon.Ids.Sfid` row continues to stand alone at `M2`.

When a row in this matrix becomes inaccurate, update both this page and the [`engine-surface-maturity-audit.md`](engine-surface-maturity-audit.md) entry in the same slice. Do not let one page drift while the other is updated.

## Maintenance posture

This document is the consolidated read across maturity audit + component catalog + runtime contract index. It is meant to evolve continuously rather than freeze. The standing rules for when to update this matrix vs. when to leave it alone live in [`planning-governance.md`](planning-governance.md) under the ["Conformance matrix maintenance"](planning-governance.md) section; this paragraph is the local reminder, not the durable rule.

- update this matrix in the same slice as the source of truth: when a package changes maturity or ownership, update both `engine-surface-maturity-audit.md` and the matching row here
- add a new row when a new shipped `Cephalon.*` package enters the repository
- delete rows when a shipped package is intentionally retired and its retirement is documented in `compatibility.md`
- when a row's `Notes` field reaches more than one line of detail, move that detail to the matching component doc and link to it instead
- review the matrix at the end of each architecture-review cycle to confirm it still matches reality

This page does not assign new policies or new requirements. It only consolidates what other docs already declare.
