# Cephalon.Eventing

> **Maturity:** `M3` · **Ownership:** mixed: `application-managed` + `cephalon-managed` — authoritative truth in [`engine-surface-maturity-audit.md`](../engine-surface-maturity-audit.md)

`Cephalon.Eventing` is the baseline technology pack for event-driven integration.

## What it owns

- eventing options
- module and registration entry points for the `EventDrivenIntegration` technology
- channel descriptors, registries, and catalogs
- declared subscription descriptors, registries, and catalogs
- configuration-owned event channel descriptors through `Engine:Messaging:Channels`, loaded by `engine.AddEventingFromConfiguration(configuration)` before module contributors so host configuration can override channel metadata by id without editing application code
- code-owned event subscription descriptors and executors through `EventingOptions.Subscriptions`, `IEventSubscriptionContributor`, `services.AddCephalonEventSubscriptionExecutor<TExecutor>()`, direct `IEventSubscriptionExecutor` registrations, `EventSubscriptionAttribute`, and optional `IEventSubscriptionDescriptorProvider`; `Engine:Messaging:Subscriptions` and `Engine:Messaging:SubscriptionHandlers` are deliberately rejected so publish/subscribe behavior stays type-safe and fast
- code-owned direct subscription execution middleware through `IEventSubscriptionExecutionMiddleware` and `EventSubscriptionExecutionStep`, giving modules MassTransit/NServiceBus/MediatR-style filters around the native in-process lane without making middleware binding string-config driven
- low-ceremony DI helpers through `services.AddCephalonEventSubscriptionExecutor<TExecutor>()` and `services.AddCephalonEventSubscriptionExecutionMiddleware<TMiddleware>()` so hosts/modules can register native executor and middleware contributions without handwritten interface registration boilerplate
- host-agnostic managed-subscription execution contracts and execution-binding vocabulary that truthful companion packs can project back into runtime introspection
- a public `IEventSubscriptionExecutionBindingCatalog` read contract for active managed subscription bindings contributed by companion packs
- an implementation of the abstraction-level `IEventSubscriptionExecutionReadinessCatalog` read contract that tells hosts and tooling whether each declared subscription is `runtime-bound`, `hosted-execution-linked`, `application-managed-state`, or `declared-only`
- stable `EventSubscriptionRuntimeMetadataKeys` constants for ownership and runtime-state fields projected through the `event-subscriptions` technology surface
- stable `EventDispatchRuntimeMetadataKeys` constants for dispatch retry, retry exhaustion, terminal failure, dispatch-store dead-letter intent, and broker-dead-letter boundary fields projected through dispatch runtime reports and event-dispatch surfaces
- an opt-in Cephalon-managed in-process subscription execution baseline through `EventingOptions.EnableInProcessSubscriptionExecution`, `IEventPublisher`, and `services.AddCephalonEventSubscriptionExecutor<TExecutor>()` or direct `IEventSubscriptionExecutor` services
- bounded process-local retry for that in-process lane through `EventingOptions.InProcessSubscriptionMaxAttempts`, `InProcessSubscriptionRetryDelayMilliseconds`, `InProcessSubscriptionRetryBackoff`, `InProcessSubscriptionRetryBackoffMultiplier`, `InProcessSubscriptionRetryMaxDelayMilliseconds`, and `InProcessSubscriptionRetryJitterPercent`, with `retry-scheduled` observations, deterministic effective-delay metadata, fixed or exponential backoff metadata when enabled, and `durable-retry-queue-ownership` profile evidence that keeps durable queue ownership separate
- bounded duplicate suppression for completed in-process subscription executions through `EventingOptions.EnableInProcessSubscriptionIdempotency`, `InProcessSubscriptionIdempotencyStore`, and `InProcessSubscriptionIdempotencyRetentionMinutes`, with either process-local or registered-`IInbox` storage, `skipped` observations, explicit idempotency store/durability/scope metadata, and `idempotency-ownership` profile evidence that keeps broader idempotency ownership separate when enabled
- `subscription-concurrency-ownership` profile evidence that keeps direct execution, code-first middleware, hosted bindings, and optional provider bindings separate from per-subscription concurrency limits, consumer prefetch, backpressure, provider concurrency, leases, and distributed work sharing
- `subscription-ordering-ownership` profile evidence that keeps direct execution, code-first middleware, hosted bindings, and optional provider bindings separate from handler ordering, local fan-out ordering, per-key ordering, partition ordering, causal ordering, replay ordering, and cross-node ordering guarantees
- an implementation of the abstraction-level `IEventPublicationDispatcher` command seam so host adapters can request one bounded event publication without referencing `Cephalon.Eventing` implementation types
- optional config-driven publication routing through `EventingOptions.EnablePublicationRouting`, `PublicationRoutingAutoChannelId`, `PublicationRoutes`, `PublicationRoutingRequireMatchedRoute`, and `PublicationRoutingRejectMismatchedExplicitChannel`, so callers can publish with a stable logical channel such as `auto` while the dispatcher resolves or validates the effective channel from event type before the active publisher runs
- optional bounded process-local scheduled/delayed publication acceptance through `EventingOptions.EnablePublicationScheduling`, `PublicationSchedulingMaxDelayMilliseconds`, and `PublicationSchedulingMaxPendingCount`, with request metadata keys `scheduledForUtc` or `delayMilliseconds`, explicit `schedule*` runtime metadata when enabled, and `scheduled-and-delayed-delivery-ownership` profile evidence that keeps durable/provider scheduling claims separate
- an implementation of the abstraction-level `IEventPublicationRuntimeCatalog` read contract so operators can inspect latest event-publication runtime state without referencing `Cephalon.Eventing` implementation types
- an optional staged publication contract through `IEventPublisher` and `EventPublication`
- application-managed dispatch runtime reporting for outbox-backed publication paths, including first-class retry-pending and terminal-failure posture on the abstraction-level state/summary contracts
- additive runtime descriptor registration for named dispatch runtimes that can be consumed through the host-agnostic `Cephalon.Abstractions/Data/*` event-dispatch contracts
- additive outbox dispatch-policy resolution so `/engine/outboxes` and runtime surfaces can distinguish `disabled`, `consumer-managed`, and runtime-managed execution ownership truthfully
- terminal dispatch-failure metadata plus typed `EventDispatchRuntimeState.TerminalFailure`, `TerminalFailureCount`, `EventDispatchRuntimeSummary.TerminalFailureCount`, `TerminalOutboxCount`, and `HasTerminalFailures` answers so dispatch stores and operators can stop poison staged publications from re-entering pending-dispatch reads when a runtime exhausts its retry budget
- a provider-neutral `event-dispatch-remediations` runtime surface plus abstraction-level `IEventDispatchRemediationDispatcher` command seam that turns reported retry-pending, skipped, failed, and terminal-failure dispatch state into bounded dispatch-store commands (`retry-now`, `retry-later`, `skip`, `quarantine`, `dead-letter`) without requiring Wolverine; broker-owned dead-letter queues remain deliberately not claimed
- an abstraction-level `IEventDispatchRemediationRuntimeCatalog` command-result read model plus bounded process-local command history controlled by `EventingOptions.RemediationCommandHistoryLimit`, including explicit retention posture through `EventDispatchRemediationRuntimeRetention`, direct retained `reserved` reads through `GetInDoubt()` / `GetInDoubtBefore(...)`, retained stale-reservation roll-ups through `GetInDoubtSummaryBefore(...)`, the oldest retained stale-reservation read through `GetOldestInDoubtBefore(...)`, inclusive observed-UTC window reads through `GetByObservedAt(...)`, and retained-window roll-ups through `GetSummaryByObservedAt(...)` whose `EventDispatchRemediationRuntimeSummary` carries reserved/in-doubt counts, the oldest retained reserved command, retention-truncation, and incomplete-summary warnings, so accepted, rejected, and reserved operator commands can be audited without inferring command history from the latest outbox state or mistaking a truncated bounded list for complete history
- baseline runtime-surface contributions for channel introspection, declared subscription introspection, truthful publication-path introspection, live-enriched dispatch-runtime introspection, live dispatch-state introspection, and managed-versus-application-owned subscription execution linkage
- the public `EventingDiagnostics` OpenTelemetry adapter declared against `CephalonActivitySources.Eventing` and `CephalonMeters.Eventing`; the in-process publisher emits one `eventing.publication.dispatch` activity per `IEventPublisher.PublishAsync` call with stable Cephalon-prefix tags (`cephalon.eventing.publisher.id`, `cephalon.eventing.publication.id`, `cephalon.eventing.channel.id`, `cephalon.eventing.event.type`, `cephalon.eventing.publication.outcome`, `cephalon.eventing.matched_subscription.count`) plus a `cephalon.eventing.publications` counter, all routed through the `RedactionPipeline` resolved from DI so consumer-registered redaction filters scrub publisher-emission attributes uniformly with the AspNetCore middleware and engine-runtime emission sites
- a reusable staged-publication seam that explicit bridge packs can hand off into without making `Cephalon.Eventing` own the upstream authoring contract
- the technology bucket that companion packs can extend with additional event-runtime truth such as outbox producers, inbox stores, or richer dispatch/subscription state
- a machine-readable `eventing-superiority-profile` technology surface and `eventing.superiority-profile` capability that compare the active Cephalon-native runtime posture against MassTransit, NServiceBus, Wolverine, and MediatR reference dimensions using explicit `claimed`, `partial`, and `not-claimed` statuses

## Main surfaces

- `Configuration/EventingOptions.cs`
- `Configuration/EventingOptionsConfigurationReader.cs`
- `Hosting/EventingServiceCollectionExtensions.cs`
- `Modules/EventingModule.cs`
- `Registration/EventingEngineBuilderExtensions.cs`
- `Services/EventChannelDescriptor.cs`
- `Services/EventChannelRegistry.cs`
- `Services/EventChannelCatalog.cs`
- `Services/IEventChannelContributor.cs`
- `Services/IEventChannelCatalog.cs`
- `Services/EventSubscriptionAttribute.cs`
- `Services/EventSubscriptionDescriptor.cs`
- `Services/EventSubscriptionExecutionContext.cs`
- `Services/EventSubscriptionExecutionStep.cs`
- `Services/IEventSubscriptionExecutionMiddleware.cs`
- `Services/EventSubscriptionExecutionBindingDescriptor.cs`
- `Services/EventSubscriptionExecutionOutcomes.cs`
- `Services/EventSubscriptionExecutionReport.cs`
- `Services/EventSubscriptionRuntimeMetadataKeys.cs`
- `Services/EventSubscriptionRuntimeState.cs`
- `Services/EventDispatchExecutionOutcomes.cs`
- `Services/EventDispatchItem.cs`
- `Services/EventDispatchExecutionReport.cs`
- `Services/EventDispatchRuntimeMetadataKeys.cs`
- `Services/IEventSubscriptionContributor.cs`
- `Services/IEventSubscriptionCatalog.cs`
- `Services/IEventSubscriptionDescriptorProvider.cs`
- `Services/IEventSubscriptionExecutor.cs`
- `Services/IEventSubscriptionExecutionBindingCatalog.cs`
- `Services/IEventSubscriptionExecutionBindingContributor.cs`
- `Services/IEventSubscriptionRuntimeCatalog.cs`
- `Services/IEventSubscriptionRuntimeReporter.cs`
- `Services/IEventDispatchRuntimeReporter.cs`
- `Services/IEventDispatchStore.cs`
- `Services/EventPublication.cs`
- `Services/EventPublicationDispatcher.cs`
- `Services/EventPublicationRoutingPolicy.cs`
- `Services/EventPublicationSchedulingPolicy.cs`
- `Services/EventPublicationScheduleQueue.cs`
- `Services/EventDispatchRemediationDispatcher.cs`
- `Services/EventDispatchRemediationRuntimeCatalog.cs`
- `Services/EventingDispatchRemediationCommandRuntimeSurfaceContributor.cs`
- `Services/IEventPublisher.cs`
- `Services/OutboxBackedEventPublisher.cs`
- `Services/OutboxDispatchPolicyCatalog.cs`
- `Services/EventDispatchRuntimeCatalog.cs`
- `Services/EventDispatchRuntimeDescriptorCatalog.cs`
- `Services/EventingRuntimeSurfaceContributor.cs`
- `Services/EventingSuperiorityProfileRuntimeSurfaceContributor.cs`
- `Services/EventingDispatchRuntimeSurfaceContributor.cs`
- `Services/EventingDispatchRemediationRuntimeSurfaceContributor.cs`
- `Services/EventingSubscriptionRuntimeSurfaceContributor.cs`
- `Services/EventingPublishingRuntimeSurfaceContributor.cs`
- `Services/EventPublicationRuntimeCatalog.cs`
- `Services/EventPublicationRuntimeReport.cs`
- `Services/IEventPublicationRuntimeReporter.cs`
- `Services/InProcessEventSubscriptionExecutorCatalog.cs`
- `Services/InProcessEventingRetryPolicy.cs`
- `Services/InProcessEventingIdempotencyPolicy.cs`
- `Services/InProcessEventSubscriptionIdempotencyTracker.cs`
- `Services/InProcessEventPublisher.cs`
- `Services/EventingDiagnostics.cs`
- `Services/EventingInProcessPublishingRuntimeSurfaceContributor.cs`
- host-agnostic read and action contracts in `Cephalon.Abstractions/Data/EventDispatchRuntimeDescriptor.cs`, `Cephalon.Abstractions/Data/EventDispatchRuntimeSummary.cs`, `Cephalon.Abstractions/Data/EventDispatchRuntimeState.cs`, `Cephalon.Abstractions/Data/IEventDispatchRuntimeCatalog.cs`, `Cephalon.Abstractions/Data/IEventDispatchRuntimeDescriptorCatalog.cs`, `Cephalon.Abstractions/Data/EventDispatchRemediationOperationIds.cs`, `Cephalon.Abstractions/Data/EventDispatchRemediationRequest.cs`, `Cephalon.Abstractions/Data/EventDispatchRemediationResult.cs`, `Cephalon.Abstractions/Data/EventDispatchRemediationRuntimeRetention.cs`, `Cephalon.Abstractions/Data/EventDispatchRemediationRuntimeSummary.cs`, `Cephalon.Abstractions/Data/EventDispatchRemediationRuntimeState.cs`, `Cephalon.Abstractions/Data/IEventDispatchRemediationDispatcher.cs`, `Cephalon.Abstractions/Data/IEventDispatchRemediationRuntimeCatalog.cs`, `Cephalon.Abstractions/Data/EventPublicationOutcomes.cs`, `Cephalon.Abstractions/Data/EventPublicationRequest.cs`, `Cephalon.Abstractions/Data/EventPublicationResult.cs`, `Cephalon.Abstractions/Data/IEventPublicationDispatcher.cs`, `Cephalon.Abstractions/Data/EventPublicationRuntimeOutcomes.cs`, `Cephalon.Abstractions/Data/EventPublicationRuntimeState.cs`, `Cephalon.Abstractions/Data/IEventPublicationRuntimeCatalog.cs`, `Cephalon.Abstractions/Data/EventSubscriptionExecutionReadinessDescriptor.cs`, `Cephalon.Abstractions/Data/EventSubscriptionExecutionReadinessStates.cs`, `Cephalon.Abstractions/Data/IEventSubscriptionExecutionReadinessCatalog.cs`, `Cephalon.Abstractions/Data/OutboxDispatchPolicyDescriptor.cs`, and `Cephalon.Abstractions/Data/IOutboxDispatchPolicyCatalog.cs`

## Source structure

- `Configuration`
- `Hosting`
- `Modules`
- `Registration`
- `Services`

## How it fits

This pack keeps event-driven primitives out of the engine core while still making them discoverable, selectable, and introspectable through the shared technology model.
Selecting `event-driven-integration` by itself should therefore produce the native `Cephalon.Eventing` baseline only; generated hosts add `Cephalon.Eventing.Wolverine`, `Engine:Messaging:Provider = Wolverine`, and `engine.AddWolverineEventing()` only when the app explicitly selects that provider-managed companion.

Wolverine is the minimum external capability benchmark for this component family. The native Cephalon eventing direction is not to wrap Wolverine more tightly, but to expose a broader provider-neutral contract that can meet or exceed Wolverine-class capabilities across durability, local and brokered transports, routing, scheduled and delayed delivery, inbound and outbound failure policy, bounded and durable retries, dead-letter and replay operations, sagas and process managers, handler/subscription discovery, serialization and versioning, idempotency, tenant/correlation context propagation, OpenTelemetry diagnostics, operator actions, runtime topology, compliance/audit evidence, and provider portability. Environment-owned policy and topology capabilities should be selected through `Engine:Messaging` or another engine-owned configuration section, while hot-path publishing and subscription behavior should remain code-first through typed contracts, surfaced through runtime catalogs and `/engine/snapshot`, and only then implemented by the native pack or an optional provider companion.

The broader extraction rule is the same for MassTransit, NServiceBus, Wolverine, MediatR, and later messaging frameworks: learn from their proven capabilities without making their packages or APIs the Cephalon authoring surface. MassTransit contributes useful reference ideas around transport topology, consumers, saga state machines, routing slips, middleware filters, outbox support, scheduling, and test harnesses. NServiceBus contributes useful reference ideas around logical endpoints, recoverability, immediate/delayed retries, error queues, outbox consistency, correlation headers, saga validation, and ServiceControl-style operations. MediatR contributes useful reference ideas around low-ceremony in-process request/response, notifications, streams, and ordered pipeline behaviors. Wolverine contributes useful reference ideas around durable inbox/outbox, scheduled delivery, generated handler pipelines, local plus external transports, error policies, diagnostics, and dead-letter replay. Cephalon should convert the useful parts into provider-neutral contracts, config-driven options, source-generated or descriptor-backed paths where helpful, and truthful runtime/operator surfaces before claiming support.

The acceptance bar is higher than feature parity. A new Cephalon eventing capability should be shaped so it beats the reference frameworks in the dimensions Cephalon controls: configuration-first adoption, swappable providers, low ceremony for app developers, runtime introspection, operator actionability, deterministic package boundaries, multi-host portability, resilience posture, observability, compliance/audit evidence, source-generated or trim-aware implementation paths where appropriate, and focused tests/benchmarks. If a dimension cannot yet beat the benchmark, the docs and maturity rows should say that plainly instead of implying full superiority.

The runtime now carries that honesty as a first-class surface. `eventing-superiority-profile` is emitted by the native pack whenever `event-driven-integration` is active, without requiring Wolverine or another provider adapter. Its entries name comparison dimensions such as configuration-first neutrality, native Wolverine-free operation, routing and provider portability, runtime truth, recoverability, outbox portability, MediatR-style local dispatch with code-first descriptor discovery, code-first subscription execution pipeline, saga/choreography posture, dead-letter/replay remediation, native remediation operator-read performance, durable remediation command audit, scheduled/delayed delivery ownership, durable retry queue ownership, idempotency ownership, subscription concurrency ownership, subscription ordering ownership, serialization/contract-versioning ownership, tenant/correlation context ownership, observability/compliance, and test/benchmark evidence. `event-dispatch-remediations` is the matching operator surface for dispatch-failure follow-through: it is populated from `IEventDispatchRuntimeCatalog` reports and projects `remediationState`, `recommendedAction`, retry metadata, terminal-failure metadata, and `operatorCommandState`. When an active `IEventDispatchStore` owns the outbox, that state becomes `bounded-dispatch-store-command-ready` and advertises `retry-now`, `retry-later`, `skip`, `quarantine`, and dispatch-store `dead-letter` through `/engine/event-dispatches/{outboxId}/commands/{operationId}`; when no mutable dispatch store exists it remains `advisory-only`. Accepted, rejected, and reserved command results are recorded into `IEventDispatchRemediationRuntimeCatalog`, summarized by `EventDispatchRemediationRuntimeSummary`, bounded by `EventDispatchRemediationRuntimeRetention`, projected through `event-dispatch-remediation-commands`, and exposed by ASP.NET Core through `/engine/event-dispatch-remediation-commands`, `/engine/event-dispatch-remediation-commands/summary`, `/engine/event-dispatch-remediation-commands/latest`, `/engine/event-dispatch-remediation-commands/retention`, `/engine/event-dispatch-remediation-commands/in-doubt?beforeUtc={beforeUtc}`, `/engine/event-dispatch-remediation-commands/in-doubt/oldest?beforeUtc={beforeUtc}`, `/engine/event-dispatch-remediation-commands/{commandId}`, `/engine/event-dispatch-remediation-commands/outboxes/{outboxId}`, `/engine/event-dispatch-remediation-commands/messages/{messageId}`, `/engine/event-dispatch-remediation-commands/channels/{channelId}`, `/engine/event-dispatch-remediation-commands/operations/{operationId}`, `/engine/event-dispatch-remediation-commands/actors/{actorId}`, `/engine/event-dispatch-remediation-commands/correlations/{correlationId}`, `/engine/event-dispatch-remediation-commands/reasons/{reason}`, `/engine/event-dispatch-remediation-commands/dispatch-outcomes/{dispatchOutcome}`, and `/engine/event-dispatch-remediation-commands/outcomes/{outcome}` plus the matching `/summary`, `/retention`, `/latest`, and `/oldest` variants for those filter families. List and drill-down routes that return command-result arrays accept `?limit={positiveInteger}` to return only the newest retained records; callers that need cursor-style reads can instead use `?pageSize={positiveInteger}&continuationToken={opaqueToken}` to receive a page envelope with `items`, `pageSize`, `returnedCount`, `totalRetainedCount`, `continuationToken`, `nextContinuationToken`, and `hasMore`, while summary/latest/retention/single-command/oldest-in-doubt/filter-summary/filter-retention/filter-latest/filter-oldest reads keep their direct contracts. `pageSize` defaults to 50, is capped at 500, and cannot be combined with the legacy `limit` response shape. Capability and runtime-surface metadata now advertises `commandListRoute`, `commandResultRoute`, `commandInDoubtRoute`, `commandInDoubtSummaryRoute`, `commandOldestInDoubtRoute`, `commandOutboxRoute`, filter-summary route keys, filter-retention route keys, filter-latest route keys, and filter-oldest route keys (with `operatorCommand*` equivalents on remediation entries) beside that read policy through `commandReadLimitQuery = limit`, `commandReadLimitPolicy = positive-integer-newest-first`, `commandReadLimitAppliesTo = list-and-filter-routes`, the matching route list, and the command outcome route. It also advertises continuation paging through `commandPaginationQuery = pageSize,continuationToken`, `commandPaginationPolicy = opaque-signed-route-bound-continuation-token-newest-first`, `commandPageSizeDefault = 50`, `commandPageSizeMaximum = 500`, route applicability, and the page response fields. It also advertises the in-doubt cutoff contract through `commandInDoubtQuery = beforeUtc`, `commandInDoubtCutoffPolicy = inclusive-observed-utc-before-or-equal`, `commandInDoubtDetailOrder = newest-first`, and `commandInDoubtInvalidCutoff = reject-invalid-date-time-offset`; the single-record oldest read advertises `commandOldestInDoubtQuery = beforeUtc`, `commandOldestInDoubtPolicy = oldest-retained-reserved-observed-utc-before-or-equal`, and `commandOldestInDoubtInvalidCutoff = reject-invalid-date-time-offset`. It also advertises the observed-window contract through `commandObservationWindowQuery = fromUtc,toUtc`, `commandObservationWindowPolicy = inclusive-observed-utc`, `commandObservationWindowDetailOrder = newest-first`, `commandObservationWindowSummary = available`, and `commandObservationWindowInvalidBounds = reject-reversed-window`; filter summaries advertise `commandFilterSummary*`, filter retention reads advertise `commandFilterRetention*`, filter latest reads advertise `commandFilterLatest*`, and filter oldest reads advertise `commandFilterOldest*` so operator UIs can discover the full native route family without probing. The dispatcher enforces `commandIdempotencyPolicy = unique-command-id` and `duplicateCommandPolicy = reject-without-mutation`: a reused command id returns a rejected result with duplicate-command metadata, does not apply another dispatch-store report, does not increase the recorded summary or retention total counts, and does not replace the original command-result record. `deadLetterCommand = dispatch-store-ready` means Cephalon can mark a staged event as a terminal dispatch-store dead-letter intent; `brokerDeadLetterCommand = not-claimed` remains explicit because broker-owned dead-letter queues are provider-specific. The `native-remediation-operator-read-performance` entry is `claimed` only when an outbox-backed command path is active; otherwise it stays `partial` while still advertising the release guardrails for `FilterSummaryByMessageId`, `FilterRetentionByMessageId`, `FilterLatestByCorrelationId`, `FilterOldestByDispatchOutcome`, and `FilterOperatorDashboardSelectors`. The `durable-remediation-command-audit` entry reads the active `IEventDispatchRemediationCommandJournal` descriptor and is `claimed` only for durable cross-node command-audit journals, `partial` for process-local journals, and `not-claimed` when no remediation command journal is active; it reports `replayCursor=durable` only when the active provider-owned command journal exposes `IEventDispatchRemediationCommandReplayCursorCatalog`, while broker replay ownership stays separate. The scheduled/delayed delivery dimension reports the bounded process-local scheduler as `partial` when enabled and keeps durable scheduled delivery, provider delay queues, broker scheduled delivery, cross-node coordination, and schedule recovery outside the claim. The durable retry queue dimension keeps retry persistence, broker error queues, poison queue ownership, cross-node retry coordination, and retry leases outside bounded in-process retry or provider retry observations. The idempotency dimension keeps broker deduplication, exactly-once delivery, durable inbox command ownership, generic inbox command processing, provider idempotency, and cross-node idempotency leases outside duplicate-completed suppression. The subscription concurrency dimension keeps per-subscription concurrency limits, parallel handler execution, consumer prefetch, backpressure, provider concurrency, leases, and distributed work sharing separate from declared subscriptions, direct in-process execution, code-first middleware, hosted binding metadata, or optional provider binding evidence. The subscription ordering dimension keeps handler ordering, local fan-out ordering, per-key ordering, partition ordering, causal ordering, replay ordering, and cross-node ordering separate from declared subscriptions, direct in-process execution, code-first middleware, hosted binding metadata, or optional provider binding evidence. The downstream delivery completion dimension keeps provider delivery receipts, subscriber acknowledgements, destination commits, and exactly-once delivery separate from publication acceptance or dispatch reports. The inbound consumption ownership dimension keeps broker consumer loops, inbound acknowledgements, provider offsets, leases, and checkpoints separate from declared subscriptions, in-process execution, inbox duplicate suppression, hosted binding metadata, or optional Wolverine binding evidence. The serialization/versioning ownership dimension keeps serializer selection, wire-envelope schema, schema registry ownership, contract-version negotiation, upcasters, and compatibility validation separate from event type names, channel metadata, routing, subscription catalogs, or publication runtime evidence. The tenant/correlation context ownership dimension keeps tenant context, correlation, causation, baggage, and message-header propagation separate from operator correlation metadata, diagnostics tags, route metadata, subscription catalogs, or publication runtime evidence. Each superiority entry reports `status`, `referenceFrameworks`, `claimPolicy`, `runtimeEvidence`, `cephalonAdvantage`, and `nextGap`, so `/engine/technology-surfaces`, `/engine/snapshot`, and `eventing.superiority-profile` can say exactly what is already better, what is partial, and what remains not claimed.

ENG-585 extends that command-result route family with `/engine/event-dispatch-remediation-commands/in-doubt/summary?beforeUtc={beforeUtc}` and `IEventDispatchRemediationRuntimeCatalog.GetInDoubtSummaryBefore(...)`. The response is an aggregate `EventDispatchRemediationRuntimeSummary` over retained `reserved` command records at or before the optional inclusive UTC cutoff, rejects invalid cutoff values at the host boundary, and stays outside list `limit` / `pageSize` contracts so dashboards can read stale in-doubt counts without materializing the detail list.

ENG-586 extends the same command-result route family with filtered summaries for outbox, message, channel, operation, actor, correlation, reason, outcome, and dispatch outcome. The new catalog methods and `/summary` filter routes return `EventDispatchRemediationRuntimeSummary` directly, so operator dashboards can render count tiles and warning posture before materializing detail pages. They do not add route-specific hot-path configuration, `limit`, `pageSize`, or continuation semantics.

ENG-587 adds the matching filtered latest reads for those same dimensions. `GetLatestByOutboxId(...)`, `GetLatestByMessageId(...)`, `GetLatestByChannelId(...)`, `GetLatestByOperationId(...)`, `GetLatestByActorId(...)`, `GetLatestByCorrelationId(...)`, `GetLatestByReason(...)`, `GetLatestByOutcome(...)`, and `GetLatestByDispatchOutcome(...)` return the newest retained `EventDispatchRemediationRuntimeState`, while `/engine/event-dispatch-remediation-commands/{filter}/{value}/latest` routes return `404` when no retained command matches. The metadata advertises `commandFilterLatest*` and per-filter latest route keys so dashboards can open the newest relevant command without materializing detail lists, installing Wolverine, or adding publish/subscribe hot-path config.

ENG-588 adds the matching filtered oldest reads for those same dimensions. `GetOldestByOutboxId(...)`, `GetOldestByMessageId(...)`, `GetOldestByChannelId(...)`, `GetOldestByOperationId(...)`, `GetOldestByActorId(...)`, `GetOldestByCorrelationId(...)`, `GetOldestByReason(...)`, `GetOldestByOutcome(...)`, and `GetOldestByDispatchOutcome(...)` return the oldest retained `EventDispatchRemediationRuntimeState`, while `/engine/event-dispatch-remediation-commands/{filter}/{value}/oldest` routes return `404` when no retained command matches. The metadata advertises `commandFilterOldest*` and per-filter oldest route keys so dashboards can inspect the first retained matching command without materializing detail lists, installing Wolverine, or adding publish/subscribe hot-path config.

ENG-589 adds the matching filtered retention reads for those same dimensions. `GetRetentionByOutboxId(...)`, `GetRetentionByMessageId(...)`, `GetRetentionByChannelId(...)`, `GetRetentionByOperationId(...)`, `GetRetentionByActorId(...)`, `GetRetentionByCorrelationId(...)`, `GetRetentionByReason(...)`, `GetRetentionByOutcome(...)`, and `GetRetentionByDispatchOutcome(...)` return the retained-history posture for the matching slice, while `/engine/event-dispatch-remediation-commands/{filter}/{value}/retention` routes return an empty `EventDispatchRemediationRuntimeRetention` when no retained command matches. The metadata advertises `commandFilterRetention*` and per-filter retention route keys so dashboards can show retained count, durable/unbounded posture, process-local truncation, and oldest/latest retained anchors without materializing detail lists, installing Wolverine, or adding publish/subscribe hot-path config.

ENG-590 hardens those filtered read families as native hot paths. The process-local catalog now computes summary and retention answers by scanning retained command history once, while latest and oldest reads keep one candidate record instead of building a matching list or sort buffer. The main string filter families use an internal enum-based matcher so latest and oldest filters avoid predicate-closure allocation; the BenchmarkDotNet guardrail covers filtered summary, retention, latest, oldest, and compact dashboard selector reads without adding route-specific configuration or requiring Wolverine.

ENG-591 projects that benchmark evidence into `eventing-superiority-profile` instead of leaving it only in the benchmark catalog. The new `native-remediation-operator-read-performance` dimension reports `claimed` when an outbox-backed command path is active, reports `partial` when the benchmarked path is available but not active in the current runtime, and advertises the exact filtered-read guardrail methods so claim maturity, operator docs, and runtime introspection line up.

ENG-592 projects the active remediation command journal descriptor into `eventing-superiority-profile`. The new `durable-remediation-command-audit` dimension reports `claimed` only when the active `IEventDispatchRemediationCommandJournal` exposes durable cross-node command audit, reports `partial` for the native process-local fallback, and reports `not-claimed` when no remediation command journal is active. Runtime evidence includes journal id, provider, storage, durability, scope, cross-node audit posture, replay-cursor posture, and `wolverineRequired=false`, so the EF durable journal can prove durable command audit without implying broker replay ownership.

ENG-593 adds the first provider-owned replay cursor under that same command-journal boundary. `IEventDispatchRemediationCommandReplayCursorCatalog` lets durable journals expose `LatestReplayCursor` and `GetAfterReplayCursor(...)` over stable `(ObservedAtUtc, CommandId)` positions in oldest-first order. The EF durable journal implements it and reports `commandJournalReplayCursor = durable`, `commandJournalReplayCursorOrder = oldest-first-observed-utc-command-id`, and `commandJournalReplayCursorScope = command-journal`; the process-local fallback still does not implement the replay-cursor catalog, and broker dead-letter replay ownership remains a separate provider-owned gap.

ENG-594 projects that cursor boundary into `eventing-superiority-profile` as `durable-command-journal-replay-cursor`. The entry reports `claimed` only when the active provider-owned command journal exposes durable command-journal replay, reports `partial` for an active journal without that catalog, and reports `not-claimed` when no remediation command journal is active. Its runtime evidence includes `brokerReplay=not-claimed` and `wolverineRequired=false` so durable command-journal replay stays separate from ASP.NET Core route paging, broker replay, and Wolverine adoption.

ENG-595 projects broker dead-letter/replay ownership into the same profile as `broker-dead-letter-replay-ownership`. That entry stays `not-claimed` until a provider package owns a broker dead-letter queue descriptor and replay action path, even when a dispatch store can record a terminal dead-letter intent or Wolverine reports a managed dispatch loop. Runtime evidence includes `dispatchStoreDeadLetterIntent`, `dispatchRuntime`, `brokerDeadLetterQueueOwnership=not-claimed`, `brokerReplay=not-claimed`, `providerOwnedBrokerPath=not-present`, and `wolverineRequired=false`, keeping broker replay separate from durable command-journal replay.

ENG-596 projects broker topology materialization into the same profile as `broker-topology-materialization-ownership`. That entry stays `not-claimed` until a provider package owns broker exchange, queue, topic, or partition provisioning/verification. When native publication routing is enabled, its evidence repeats the Cephalon route policy and route count beside `brokerTopologyMaterialization=not-claimed`, `exchangeProvisioning=not-claimed`, `queueProvisioning=not-claimed`, `topicProvisioning=not-claimed`, `partitionOwnership=not-claimed`, `providerOwnedTopology=not-present`, and `wolverineRequired=false`, so route/channel governance cannot be mistaken for broker topology ownership.

ENG-597 projects provider partition ownership into the same profile as `provider-partition-ownership`. That entry stays `not-claimed` until a provider package owns partition assignment, affinity, rebalancing, and ordering semantics. When native publication routing is enabled, its evidence repeats the Cephalon route policy and route count beside `providerPartitionOwnership=not-claimed`, `partitionAssignment=not-claimed`, `partitionAffinity=not-claimed`, `partitionRebalancing=not-claimed`, `partitionOrderingGuarantee=not-claimed`, `providerOwnedPartitioning=not-present`, and `wolverineRequired=false`, so route selection and broker topology posture cannot be mistaken for provider partition ownership.

ENG-598 projects downstream delivery completion into the same profile as `downstream-delivery-completion-ownership`. That entry stays `not-claimed` until a provider package owns destination delivery receipts, subscriber acknowledgement, destination commits, and exactly-once completion evidence. Its runtime evidence includes `publicationPath`, `handoff`, `dispatchRuntime`, `downstreamDeliveryCompletion=not-claimed`, `providerDeliveryReceipt=not-present`, `subscriberAcknowledgement=not-claimed`, `destinationCommit=not-claimed`, `exactlyOnceDelivery=not-claimed`, and `wolverineRequired=false`, so outbox accepted handoff, dispatch reports, and provider-managed dispatch cannot be mistaken for completed downstream delivery.

ENG-599 projects inbound broker consumption into the same profile as `broker-inbound-consumption-ownership`. That entry stays `not-claimed` until a provider package owns a broker consumer loop, inbound acknowledgements, consumer leases, retry/poison handling, and offset or checkpoint evidence. Its runtime evidence includes declared subscription, in-process execution, managed binding, external binding, and inbox-path posture beside `brokerInboundConsumption=not-claimed`, `brokerConsumerLoop=not-present`, `providerOwnedConsumer=not-present`, `inboundAcknowledgement=not-claimed`, `consumerOffsetCheckpoint=not-claimed`, and `wolverineRequired=false`, so declared subscriptions, direct execution, inbox duplicate suppression, and optional provider bindings cannot be mistaken for generic broker consumption ownership.

ENG-600 projects serialization and contract-version ownership into the same profile as `serialization-and-contract-versioning-ownership`. That entry stays `not-claimed` until a provider or engine package owns serializer selection, a stable wire-envelope schema, schema registry identity, event contract-version negotiation, upcaster pipelines, and compatibility validation. Its runtime evidence reports channel catalog, subscription catalog, publication path, and publication routing posture beside `serializerSelection=not-claimed`, `messageEnvelopeSchema=not-claimed`, `schemaRegistry=not-present`, `contractVersionNegotiation=not-claimed`, `upcasterPipeline=not-present`, `compatibilityValidation=not-claimed`, and `wolverineRequired=false`, so event type names, route metadata, subscription descriptors, and publication observations cannot be mistaken for wire-contract ownership.

ENG-601 projects tenant and correlation context ownership into the same profile as `tenant-and-correlation-context-ownership`. That entry stays `not-claimed` until a provider or engine package owns tenant context propagation, correlation context propagation, causation identifiers, baggage propagation, and message-header policy. Its runtime evidence reports channel catalog, subscription catalog, publication path, publication routing, and in-process execution posture beside `operatorCorrelationMetadata=metadata-only`, `tenantContextPropagation=not-claimed`, `correlationContextPropagation=not-claimed`, `causationIdPropagation=not-claimed`, `baggagePropagation=not-claimed`, `messageHeaderPolicy=not-claimed`, and `wolverineRequired=false`, so remediation command actor/correlation fields, diagnostic tags, route metadata, subscription descriptors, and publication observations cannot be mistaken for context propagation ownership.

ENG-602 projects scheduled and delayed delivery ownership into the same profile as `scheduled-and-delayed-delivery-ownership`. That entry reports `partial` only when the bounded process-local publication scheduler is enabled and otherwise stays `not-claimed`; its runtime evidence names `publicationScheduling`, `schedulePolicy`, `processLocalScheduleQueue`, delay and pending-count bounds, and `scheduleDurability` beside `durableScheduledDelivery=not-claimed`, `providerDelayQueue=not-present`, `brokerScheduledDelivery=not-claimed`, `crossNodeScheduleCoordination=not-claimed`, `scheduleRecovery=not-claimed`, and `wolverineRequired=false`, so local delayed acceptance cannot be mistaken for durable scheduled delivery or provider-owned broker delay queues.

ENG-604 projects idempotency ownership into the same profile as `idempotency-ownership`. That entry reports `partial` only when completed-publication duplicate suppression is enabled and otherwise stays `not-claimed`; its runtime evidence names the active policy, store, scope, durability, key shape, retention window, inbox path, and completed-execution duplicate suppression beside `brokerDeduplication=not-claimed`, `exactlyOnceDelivery=not-claimed`, `durableInboxCommandOwnership=not-claimed`, `genericInboxCommandOwnership=not-claimed`, `crossNodeIdempotencyLease=not-claimed`, `providerIdempotency=not-claimed`, and `wolverineRequired=false`, so process-local or inbox-backed duplicate-completed suppression cannot be mistaken for broker deduplication, exactly-once delivery, or provider-owned idempotency semantics.

ENG-605 projects subscription concurrency ownership into the same profile as `subscription-concurrency-ownership`. That entry stays `not-claimed` until a provider or engine package owns per-subscription concurrency limits, parallel handler execution, consumer prefetch, backpressure, provider concurrency, consumer leases, and distributed work sharing. Its runtime evidence names declared subscription, direct execution, code-first middleware, managed binding, and external binding posture beside `subscriptionConcurrency=not-claimed`, `perSubscriptionConcurrencyLimit=not-claimed`, `parallelHandlerExecution=not-claimed`, `consumerPrefetch=not-claimed`, `backpressure=not-claimed`, `providerConcurrency=not-present`, `consumerLease=not-claimed`, `workStealing=not-claimed`, and `wolverineRequired=false`, so direct in-process execution and middleware cannot be mistaken for provider-grade concurrency or backpressure ownership.

ENG-606 projects subscription ordering ownership into the same profile as `subscription-ordering-ownership`. That entry stays `not-claimed` until a provider or engine package owns handler ordering, local fan-out ordering, per-key ordering, partition ordering, causal ordering, replay ordering, and cross-node ordering guarantees. Its runtime evidence names declared subscription, direct execution, code-first middleware, managed binding, and external binding posture beside `subscriptionOrdering=not-claimed`, `handlerOrderingGuarantee=not-claimed`, `localFanOutOrdering=not-claimed`, `perKeyOrdering=not-claimed`, `partitionOrdering=not-claimed`, `causalOrdering=not-claimed`, `replayOrdering=not-claimed`, `crossNodeOrdering=not-claimed`, `providerOrdering=not-present`, and `wolverineRequired=false`, so local execution and provider binding metadata cannot be mistaken for ordering ownership.

The command-result technology surface now emits a stable catalog entry with id `event-dispatch-remediation-commands` before any command history exists. That catalog entry reports `entryKind = catalog`, list/result/in-doubt/in-doubt-summary/oldest-in-doubt/outbox route discovery, filter-summary route discovery, filter-retention route discovery, filter-latest route discovery, filter-oldest route discovery, route/read-limit/pagination/window/in-doubt-cutoff/idempotency policy, `commandStateCount`, `commandHistoryLimit`, retained/recorded/dropped counts, truncation posture, and `wolverineRequired = false`, while recorded commands remain separate `entryKind = command-result` entries. This gives operator UIs a native, Wolverine-free route-family and retention contract without waiting for a command to be recorded or probing ASP.NET Core routes.

Capability metadata, `event-dispatch-remediations`, and `event-dispatch-remediation-commands` now populate that command route, in-doubt route, in-doubt summary route, oldest in-doubt route, filter-summary routes, filter-retention routes, filter-latest routes, filter-oldest routes, in-doubt cutoff query, read-limit, pagination, observed-window, operation-id, and idempotency metadata through one internal Eventing-owned helper. The helper stays internal so no public route-constant API is created, but future route-family metadata changes should update that helper first and keep focused parity coverage aligned.

ENG-579 strengthens the remediation command idempotency story from result-only duplicate detection to reservation-before-mutation. `IEventDispatchRemediationCommandJournal.ReserveAsync(...)` is called before a dispatch-store report is applied, and durable journals persist a visible `reserved` command record that is overwritten by the final accepted/rejected result. If the process fails after reservation but before finalization, a later retry sees the same command id as reserved, returns a rejected duplicate response, reports `existingCommandOutcome = reserved`, and does not mutate dispatch state. Metadata now advertises `commandReservationPolicy = reserve-before-mutation`, `commandReservationTiming = before-dispatch-store-mutation`, `commandReservationState`, `commandReservationDuplicatePolicy`, `commandReservationInDoubtOutcome = reserved`, and `commandReservationOwner = command-journal` beside the existing `unique-command-id` and `reject-without-mutation` keys.

ENG-580 makes the in-doubt state visible in the aggregate contract instead of forcing dashboards to scan command records. `EventDispatchRemediationRuntimeSummary` now exposes `ReservedCount` and `HasInDoubtCommands`, and the `event-dispatch-remediation-commands` catalog metadata mirrors them through `summaryReservedCount` and `summaryHasInDoubtCommands`. ENG-581 adds the age anchor for that same posture: `OldestReservedCommandId` and `OldestReservedObservedAtUtc` point at the oldest retained reserved command visible to the summary, and catalog metadata mirrors them through `summaryOldestReservedCommandId` and `summaryOldestReservedObservedAtUtc`. ENG-582 adds the direct read alias for that posture: `IEventDispatchRemediationRuntimeCatalog.GetInDoubt()` and `/engine/event-dispatch-remediation-commands/in-doubt` return retained `reserved` records newest-first with the same limit and continuation paging contract as other list/filter routes. ENG-583 adds the stale cutoff filter: `GetInDoubtBefore(...)` and `/engine/event-dispatch-remediation-commands/in-doubt?beforeUtc={beforeUtc}` return only retained `reserved` records observed at or before the inclusive UTC cutoff, and invalid cutoff values fail with `400`. ENG-584 adds the single-record stale read: `GetOldestInDoubtBefore(...)` and `/engine/event-dispatch-remediation-commands/in-doubt/oldest?beforeUtc={beforeUtc}` return the oldest retained `reserved` command at or before the cutoff, return `404` when none is retained, and reject invalid cutoffs with `400`. ENG-585 adds the aggregate stale read: `GetInDoubtSummaryBefore(...)` and `/engine/event-dispatch-remediation-commands/in-doubt/summary?beforeUtc={beforeUtc}` return retained `reserved` counts and oldest-reserved posture for the optional cutoff without forcing a detail-list materialization. ENG-586 adds filter-specific aggregate reads so outbox/message/channel/operation/actor/correlation/reason/outcome/dispatch-outcome dashboards can get the same summary shape without materializing retained command rows first. ENG-587 adds the matching newest retained command read for each filter family, ENG-588 adds the matching oldest retained command read, and ENG-589 adds the matching retained-history posture read. ENG-590 keeps those filtered summary, retention, latest, and oldest reads single-pass and benchmark-guarded in the native process-local catalog, and ENG-591 projects that proof into `eventing-superiority-profile` through the `native-remediation-operator-read-performance` dimension. Both process-local and durable EF journals derive those values from authoritative `reserved` command records, while duplicate HTTP retries still stay outside the command-history counts.

Today that baseline is still deliberately modest and intentionally truthful. `Cephalon.Eventing` owns channel descriptors plus the `event-channels` runtime surface, exposes declared subscription descriptors through `event-subscriptions` and `eventing.subscriptions`, loads channel metadata from `Engine:Messaging:Channels`, and keeps subscription descriptors, descriptor-bearing executors, and execution middleware code-owned through typed contracts. `engine.AddEventingFromConfiguration(configuration)` still rejects `Engine:Messaging:Subscriptions`, `Engine:Messaging:EventSubscriptions`, `Engine:Messaging:SubscriptionHandlers`, and `Engine:Messaging:EventSubscriptionHandlers` because hot-path publish/subscribe behavior should stay typed and fast.

The command-result route family also includes `/engine/event-dispatch-remediation-commands/observations?fromUtc={fromUtc}&toUtc={toUtc}` and `/engine/event-dispatch-remediation-commands/observations/summary?fromUtc={fromUtc}&toUtc={toUtc}`. They are backed by `IEventDispatchRemediationRuntimeCatalog.GetByObservedAt(...)` and `GetSummaryByObservedAt(...)`, filter retained records by an inclusive observed UTC window, reject reversed bounds, and let incident-timeline reads choose between latest-first detail records and aggregate accepted/rejected/reserved/error posture without requiring Wolverine or a broker-specific audit store. The detail route also accepts `&limit={positiveInteger}` or the same `&pageSize={positiveInteger}&continuationToken={opaqueToken}` cursor-style read mode after the window parameters so dashboards can walk the newest retained incident records without adding another catalog abstraction. Summary responses also carry `reservedCount`, `hasInDoubtCommands`, `oldestReservedCommandId`, `oldestReservedObservedAtUtc`, `droppedCommandCount`, `retentionTruncated`, `summaryMayBeIncomplete`, `oldestRetainedCommandId`, and `oldestRetainedObservedAtUtc` so dashboards can warn when a requested window has in-doubt commands, identify the oldest retained in-doubt command, or detect that the window crosses before the retained cutoff without issuing a separate retention read. Capability and runtime-surface metadata exposes the same `fromUtc,toUtc` query names, inclusive observed-UTC policy, latest-first detail ordering, summary availability, and reversed-window rejection posture.

The pack exposes host-agnostic read/action seams for subscription execution readiness, bounded event-publication actions, publication runtime state, dispatch runtime state, dispatch remediation commands, and remediation command-result history. `EventSubscriptionRuntimeMetadataKeys` keeps `event-subscriptions` ownership, readiness, binding, and `reported.*` keys stable. `EventDispatchRuntimeMetadataKeys` now covers retry keys such as `nextRetryAtUtc`, `retryPolicy`, `retryMaxAttempts`, `retryDelaySeconds`, `retryDurability`, `retryScope`, `retryOutcome`, `retryExhausted`, `terminalFailure`, plus dispatch-store dead-letter keys `deadLetterOutcome`, `deadLetterScope`, `deadLetterDurability`, and the broker-boundary key `brokerDeadLetter`. `EventDispatchRemediationMetadataKeys` keeps command-result idempotency, actor, correlation, and reason metadata stable, including `operatorActorId`, `operatorCorrelationId`, `operatorCommandReason`, `commandIdempotencyPolicy`, `duplicateCommandPolicy`, `duplicateCommand`, and existing-command readback keys for rejected duplicate requests.

The `event-dispatch-remediations` technology surface reads `IEventDispatchRuntimeCatalog` and emits only actionable retry-pending, skipped, failed, or terminal states. When an `IEventDispatchStore` owns the outbox, `IEventDispatchRemediationDispatcher` can apply `retry-now`, `retry-later`, `skip`, `quarantine`, and `dead-letter` as durable dispatch-store outcomes. The `dead-letter` operation marks the staged event as a terminal dispatch-store dead-letter intent with `brokerDeadLetter = false`; it does not create, inspect, or replay a provider-owned broker DLQ. A host without Wolverine therefore gets provider-neutral remediation posture, bounded operator commands, stable reported metadata, and explicit broker boundary evidence without needing a broker-specific dead-letter queue.

The `durable-retry-queue-ownership` dimension reports bounded in-process retry evidence without claiming durable retry queues. It stays `not-claimed` until a provider or engine package owns retry persistence, broker error queues, poison queue ownership, cross-node retry coordination, and retry leases.

The `idempotency-ownership` dimension reports process-local or inbox-backed completed-execution duplicate suppression without claiming wider idempotency guarantees. It is `partial` only while `EnableInProcessSubscriptionIdempotency` is enabled and keeps broker deduplication, exactly-once delivery, generic inbox command ownership, durable inbox command ownership, provider idempotency, and cross-node idempotency leases outside the core pack until a package owns those paths.

The `subscription-concurrency-ownership` dimension reports direct in-process execution, code-first middleware count, and managed binding posture without claiming wider concurrency control. It stays `not-claimed` until a package owns per-subscription concurrency limits, parallel handler execution, consumer prefetch, backpressure, provider concurrency, leases, and distributed work sharing.

The `subscription-ordering-ownership` dimension reports direct in-process execution, code-first middleware count, and managed binding posture without claiming wider ordering guarantees. It stays `not-claimed` until a package owns handler ordering, local fan-out ordering, per-key ordering, partition ordering, causal ordering, replay ordering, and cross-node ordering.

When `EnableInProcessSubscriptionExecution` is set and at least one `IEventSubscriptionExecutor` is registered, the core pack contributes a Cephalon-managed direct in-process binding, exposes `eventing.publish` with `handoff = in-process`, invokes matching executors through `IEventPublisher`, and reports started/succeeded/failed observations through the same subscription runtime catalog. Code-first `IEventSubscriptionExecutionMiddleware` services compose around that direct lane, bounded process-local retry and idempotency metadata remain explicit, and inbox-backed duplicate-completed suppression uses exactly one registered `IInbox` when configured. When a real `IOutbox`-backed handoff path exists and the in-process path is not selected, the pack reports publication state as `accepted` with `handoff = outbox` and keeps downstream dispatch completion separate in the event-dispatch runtime surfaces.

When `EnablePublicationScheduling` is set, the publication dispatcher can also accept one delayed publication request without Wolverine. Callers express that request through publication metadata with either `scheduledForUtc` or `delayMilliseconds`; the dispatcher validates the configured delay and pending-count bounds, reports an immediate `accepted` publication runtime state with `schedulePolicy = bounded-process-local`, and then hands the publication to the active `IEventPublisher` when the local timer becomes due. Runtime metadata includes `scheduleState`, `scheduleScope`, `scheduleDurability`, `scheduleSource`, `scheduledForUtc`, `scheduleDelayMilliseconds`, and `scheduledPublicationPendingCount`, while the `event-publishers` technology surface reports the active policy, max delay, max pending count, current pending count, and next due time. The `scheduled-and-delayed-delivery-ownership` profile entry marks that lane as only `partial` and keeps durable scheduled delivery, provider delay queues, broker scheduled delivery, cross-node schedule coordination, and schedule recovery explicit as unclaimed provider/engine follow-through.

When `EnablePublicationRouting` is set, the publication dispatcher resolves routing before scheduling or publishing. Requests whose `channelId` equals `PublicationRoutingAutoChannelId` (default `auto`) must match `PublicationRoutes`; exact event-type keys match first and trailing-`*` keys match by prefix. The dispatcher then creates the effective publication on the configured route channel and records `routingPolicy = event-type-map`, `routingState`, `routingSource`, `routingRequestedChannelId`, `routingEffectiveChannelId`, `routingRule`, and `routingRuleChannelId` metadata. Explicit channel requests can be allowed, required to match a route, or rejected when they disagree with the configured route, depending on `PublicationRoutingRequireMatchedRoute` and `PublicationRoutingRejectMismatchedExplicitChannel`. This is provider-neutral publication routing and channel-governance truth; broker-specific exchanges, queues, topics, partitions, topology materialization, partition assignment, partition affinity, partition rebalancing, per-partition ordering guarantees, inbound broker consumption, inbound acknowledgements, offset checkpoints, downstream delivery completion, provider delivery receipts, subscriber acknowledgements, destination commits, exactly-once delivery guarantees, serializer selection, wire-envelope schema, schema registry ownership, contract-version negotiation, upcasters, compatibility validation, tenant context propagation, correlation/causation propagation, baggage propagation, message-header policy, per-subscription concurrency limits, consumer prefetch, backpressure, provider concurrency, consumer leases, distributed work sharing, handler ordering, local fan-out ordering, per-key ordering, causal ordering, replay ordering, and cross-node ordering still belong to future engine/provider contract work or provider companion packs. `broker-topology-materialization-ownership`, `provider-partition-ownership`, `downstream-delivery-completion-ownership`, `broker-inbound-consumption-ownership`, `serialization-and-contract-versioning-ownership`, `tenant-and-correlation-context-ownership`, `subscription-concurrency-ownership`, and `subscription-ordering-ownership` are the matching profile guardrails that keep those broker/provider/wire-contract/context/concurrency/ordering claims machine-readable and `not-claimed` until a package owns them.

The command-result read model is intentionally separate from the dispatch-state catalog. `IEventDispatchRuntimeCatalog` answers the latest state of an outbox dispatch path, while `IEventDispatchRemediationRuntimeCatalog` answers which operator commands were accepted, rejected, or still in doubt, including command id, outbox id, message id, channel id, operation, actor, correlation id, reason, outcome, dispatch outcome, timestamp, error, safe metadata, the `Summary` roll-up, the `Latest` authoritative command record, the `Retention` posture, `GetInDoubt()` for retained `reserved` records, `GetInDoubtBefore(...)` for retained `reserved` records at or before an inclusive UTC cutoff, `GetOldestInDoubtBefore(...)` for the single oldest retained stale reservation, filter-summary roll-ups, filter-retention reads, filter-latest reads, and filter-oldest reads for retained outbox, message, channel, operation, actor, correlation, reason, outcome, and dispatch-outcome slices. `IEventDispatchRemediationCommandJournal` is the active write/read contract under that catalog shape: the dispatcher checks it for duplicate command ids, records accepted/rejected results through it, and ASP.NET Core remediation-command read routes prefer it when present. `Cephalon.Eventing` ships a process-local bounded journal fallback so Wolverine is never required; provider packs can replace that journal without changing app command code or route consumers. `Cephalon.Data.EntityFramework` now supplies a durable implementation when the write-side `DbContext` implements `IEntityFrameworkEventDispatchRemediationCommandJournalContext`, maps the shared entity with `ConfigureCephalonEventDispatchRemediationCommandJournal()`, and the outbox path is registered. In that durable lane, duplicate command-id rejection survives service-provider/process rebuilds after the original command result is recorded, `Retention.HistoryLimit = 0` means unbounded provider-owned history, `IEventDispatchRemediationCommandReplayCursorCatalog` exposes oldest-first command-journal reads after a stable `(ObservedAtUtc, CommandId)` cursor, and `commandJournalDurability = durable`, `commandJournalScope = cross-node`, `commandCrossNodeCommandAudit = true`, `commandJournalReplayCursor = durable`, `commandJournalReplayCursorOrder = oldest-first-observed-utc-command-id`, and `commandJournalReplayCursorScope = command-journal` make the posture explicit. `eventing-superiority-profile` mirrors the same descriptor through `durable-remediation-command-audit`, so durable command audit and command-journal replay cursor support can be claimed independently from broker dead-letter ownership.

`GetInDoubtSummaryBefore(...)` sits beside those reads as the aggregate stale-reservation query: it returns the retained `reserved` roll-up for an optional inclusive cutoff and backs `/engine/event-dispatch-remediation-commands/in-doubt/summary?beforeUtc={beforeUtc}` without list pagination or read-limit semantics.

`IEventDispatchRemediationCommandReplayCursorCatalog` is deliberately narrower than the ASP.NET Core list continuation tokens. It is a provider-journal contract for walking durable command records after a stable `(ObservedAtUtc, CommandId)` cursor in oldest-first order, while route paging remains newest-first operator navigation and broker replay/DLQ replay remains provider-specific work.

`eventing-superiority-profile` reports that same boundary in separate dimensions on purpose: `durable-remediation-command-audit` answers whether the journal is durable cross-node audit, `durable-command-journal-replay-cursor` answers whether the active provider exposes durable command-journal replay, `broker-dead-letter-replay-ownership` answers whether a provider owns broker DLQ/replay, `broker-topology-materialization-ownership` answers whether a provider owns broker topology provisioning or verification, `provider-partition-ownership` answers whether a provider owns partition assignment, affinity, rebalancing, and ordering semantics, `downstream-delivery-completion-ownership` answers whether a provider owns destination delivery receipts, subscriber acknowledgement, destination commits, and exactly-once completion evidence, `broker-inbound-consumption-ownership` answers whether a provider owns a broker consumer loop, inbound acknowledgements, leases, retries, and offset checkpoints, `serialization-and-contract-versioning-ownership` answers whether a provider or engine package owns serializer selection, schema registry identity, version negotiation, upcasters, and compatibility validation, `tenant-and-correlation-context-ownership` answers whether a provider or engine package owns tenant context propagation, correlation/causation propagation, baggage propagation, and message-header policy, `scheduled-and-delayed-delivery-ownership` answers whether the runtime only owns bounded process-local delayed acceptance or can prove durable scheduled delivery, `subscription-concurrency-ownership` answers whether a provider or engine package owns concurrency limits, prefetch, backpressure, provider concurrency, leases, and distributed work sharing, and `subscription-ordering-ownership` answers whether a provider or engine package owns handler, local fan-out, per-key, partition, causal, replay, or cross-node ordering. Broker replay, topology, provider partitioning, downstream delivery completion, inbound broker consumption, wire-contract/versioning ownership, context propagation, durable scheduled delivery, provider delay queues, cross-node schedule coordination, schedule recovery, subscription concurrency ownership, and subscription ordering ownership remain outside the durable journal, routing, publication accepted handoff, dispatch-report, subscription descriptor, operator command metadata, local scheduler, middleware pipeline, and direct in-process execution claims until a package owns them.

`durable-retry-queue-ownership` is separate from recoverability on purpose: bounded in-process retry, dispatch runtime reports, and provider-managed retry observations may be visible, but durable retry queues, retry persistence, broker error queues, poison queue ownership, cross-node retry coordination, and retry leases remain outside the Cephalon core claim until a package owns those paths explicitly.

`idempotency-ownership` is separate from duplicate-completed suppression on purpose: process-local and inbox-backed completed-publication checks may be active, but broker deduplication, exactly-once delivery, generic inbox command processing, durable inbox command ownership, provider idempotency semantics, and cross-node idempotency leases remain outside the Cephalon core claim until a package owns those paths explicitly.

`subscription-concurrency-ownership` is separate from direct execution on purpose: declared subscriptions, registered executors, code-first middleware, hosted bindings, and optional provider binding metadata may be visible, but per-subscription concurrency limits, parallel handler execution, consumer prefetch, backpressure, provider concurrency, consumer leases, and distributed work sharing remain outside the Cephalon core claim until a package owns those paths explicitly.

`subscription-ordering-ownership` is separate from direct execution on purpose: declared subscriptions, registered executors, code-first middleware, hosted bindings, and optional provider binding metadata may be visible, but handler ordering, local fan-out ordering, per-key ordering, partition ordering, causal ordering, replay ordering, and cross-node ordering remain outside the Cephalon core claim until a package owns those paths explicitly.

The filter-summary methods `GetSummaryByOutboxId(...)`, `GetSummaryByMessageId(...)`, `GetSummaryByChannelId(...)`, `GetSummaryByOperationId(...)`, `GetSummaryByActorId(...)`, `GetSummaryByCorrelationId(...)`, `GetSummaryByReason(...)`, `GetSummaryByOutcome(...)`, and `GetSummaryByDispatchOutcome(...)` sit beside the list drill-downs for operator dashboards that need counts before opening detail pages. ASP.NET Core maps the same contract under `/engine/event-dispatch-remediation-commands/{filter}/{value}/summary` route shapes for outboxes, messages, channels, operations, actors, correlations, reasons, outcomes, and dispatch outcomes. These routes return `EventDispatchRemediationRuntimeSummary`, return an empty summary for no retained match, and stay outside `limit`, `pageSize`, and continuation-token contracts because they are direct aggregate reads through the active process-local or durable journal. The native process-local implementation keeps filtered summaries as one retained-history scan that allocates only the returned summary object.

The filter-retention methods `GetRetentionByOutboxId(...)`, `GetRetentionByMessageId(...)`, `GetRetentionByChannelId(...)`, `GetRetentionByOperationId(...)`, `GetRetentionByActorId(...)`, `GetRetentionByCorrelationId(...)`, `GetRetentionByReason(...)`, `GetRetentionByOutcome(...)`, and `GetRetentionByDispatchOutcome(...)` sit beside those summaries for dashboards that need retained-history posture before opening detail pages. ASP.NET Core maps the same contract under `/engine/event-dispatch-remediation-commands/{filter}/{value}/retention` route shapes for outboxes, messages, channels, operations, actors, correlations, reasons, outcomes, and dispatch outcomes. These routes return `EventDispatchRemediationRuntimeRetention`, return an empty retention posture for no retained match, and stay outside `limit`, `pageSize`, and continuation-token contracts because they are direct retention reads through the active process-local or durable journal. The native process-local implementation keeps filtered retention reads as one retained-history scan that allocates only the returned retention object.

The filter-latest methods `GetLatestBy*` and filter-oldest methods `GetOldestBy*` sit beside those summaries for dashboards that need one retained command record without opening the detail list. ASP.NET Core maps them under `/engine/event-dispatch-remediation-commands/{filter}/{value}/latest` and `/engine/event-dispatch-remediation-commands/{filter}/{value}/oldest`; both route families return `EventDispatchRemediationRuntimeState` or `404` and stay outside `limit`, `pageSize`, and continuation-token contracts because they are direct single-record reads through the active process-local or durable journal. The native process-local implementation keeps filtered latest and oldest reads allocation-free for the main string filter families by scanning once and retaining one candidate record instead of allocating a matching list, sort buffer, or predicate closure.

The summary gives operators total/accepted/rejected/error/duplicate-command/reserved counts plus `HasInDoubtCommands`, the oldest retained reserved command id and timestamp, the latest command id, operation, outcome, dispatch outcome, observation time, dropped-command count, retention-truncated flag, incomplete-summary flag, and oldest retained cutoff without materializing command-history detail lists; `GetSummaryByObservedAt(...)` gives the same roll-up for an observed UTC window when dashboards only need incident-window counts and sets `summaryMayBeIncomplete` when a bounded catalog may have dropped matching records before `oldestRetainedObservedAtUtc`. `GetInDoubt()` returns retained `reserved` command records newest-first when operators need the actual stuck reservations; `GetInDoubtBefore(...)` narrows that list to records observed at or before a UTC cutoff so dashboards can ask for stale reservations directly; `GetInDoubtSummaryBefore(...)` returns the retained matching `reserved` summary so dashboards can read stale counts and oldest retained reserved posture directly; `GetOldestInDoubtBefore(...)` returns one oldest retained matching reservation so dashboards can open the next aging incident without materializing the full in-doubt set; `Latest` returns the newest authoritative command-state record directly when operators need the full metadata/error payload; `Retention` returns the configured history limit, retained count, total recorded count, dropped count, truncation flag, oldest retained command, and latest retained command so operators can tell whether the active journal is bounded process-local history or provider-owned durable history. Command ids are unique operator action ids, not reusable operation names. Operation ids are stable action families, so operators can drill into all recorded `retry-now`, `retry-later`, `skip`, `quarantine`, or `dead-letter` command results without parsing the latest dispatch-state metadata. Message ids and channel ids are stable event routing coordinates, so operators can inspect every command result that touched one event message or channel without scanning all command history. Actor ids, correlation ids, and reasons are optional command metadata; when supplied, `/engine/event-dispatch-remediation-commands/actors/{actorId}`, `/engine/event-dispatch-remediation-commands/correlations/{correlationId}`, and `/engine/event-dispatch-remediation-commands/reasons/{reason}` return the accepted, rejected, and reserved command results recorded for that audit dimension without treating duplicate rejected command-id attempts as a second command-history entry. Dispatch outcomes are the dispatch-store observations produced by accepted or rejected command attempts, so `/engine/event-dispatch-remediation-commands/dispatch-outcomes/{dispatchOutcome}` can show every recorded command that produced `retry-scheduled`, `skipped`, or `failed` without scanning all command history. If a client retries with a command id that is already reserved or recorded, the dispatcher returns `rejected` with `duplicateCommand = true`, `existingCommandOutcome`, `existingCommandOperationId`, `existingCommandOutboxId`, and `existingCommandObservedAtUtc`; the original reserved/final command record remains authoritative, summary and retention counts remain tied to authoritative command records, `Latest` continues to point at the authoritative newest command instead of the duplicate attempt, and dispatch-state mutation is skipped. ASP.NET Core exposes those records through `/engine/event-dispatch-remediation-commands*`, including `/engine/event-dispatch-remediation-commands/summary`, `/engine/event-dispatch-remediation-commands/latest`, `/engine/event-dispatch-remediation-commands/retention`, `/engine/event-dispatch-remediation-commands/in-doubt?beforeUtc={beforeUtc}`, `/engine/event-dispatch-remediation-commands/in-doubt/summary?beforeUtc={beforeUtc}`, `/engine/event-dispatch-remediation-commands/in-doubt/oldest?beforeUtc={beforeUtc}`, `/engine/event-dispatch-remediation-commands/observations?fromUtc={fromUtc}&toUtc={toUtc}`, `/engine/event-dispatch-remediation-commands/observations/summary?fromUtc={fromUtc}&toUtc={toUtc}`, `/engine/event-dispatch-remediation-commands/messages/{messageId}`, `/engine/event-dispatch-remediation-commands/channels/{channelId}`, `/engine/event-dispatch-remediation-commands/operations/{operationId}`, `/engine/event-dispatch-remediation-commands/actors/{actorId}`, `/engine/event-dispatch-remediation-commands/correlations/{correlationId}`, `/engine/event-dispatch-remediation-commands/reasons/{reason}`, `/engine/event-dispatch-remediation-commands/dispatch-outcomes/{dispatchOutcome}`, and `/engine/event-dispatch-remediation-commands/outcomes/{outcome}` plus matching filter `/summary`, `/retention`, `/latest`, and `/oldest` route variants, and the technology-surface entry `event-dispatch-remediation-commands` keeps the same command-result truth available to snapshot consumers through `TechnologySurfaces`.

For incident timelines, `GetByObservedAt(...)` plus `/engine/event-dispatch-remediation-commands/observations?fromUtc={fromUtc}&toUtc={toUtc}` return only authoritative command records whose `ObservedAtUtc` falls inside the optional inclusive bounds visible to the active journal. `GetSummaryByObservedAt(...)` plus `/engine/event-dispatch-remediation-commands/observations/summary?fromUtc={fromUtc}&toUtc={toUtc}` return the window roll-up without forcing an operator UI to materialize the list first, and carry the retained-history cutoff plus `summaryMayBeIncomplete` when the active journal is bounded and the requested lower bound is open or older than the oldest retained command after truncation. They deliberately do not resurrect dropped records after bounded-history truncation; the `/retention` route remains the full retention posture when operators need recorded/dropped/retained counts and latest retained command detail. Continuation tokens are opaque ASP.NET Core operator-route paging tokens over the same newest-first ordering, work on list/filter and observation detail routes, carry a versioned route/filter scope hash, and include a process-local HMAC-SHA256 signature over the cursor payload so a token from one route or observed-window filter is rejected on another route and a tampered token is rejected instead of returning a misleading page. They are not a durable cross-node replay cursor; durable journal implementations must advertise a replay-cursor contract separately before operators can treat route cursors as replay state.

Native eventing reads environment-owned channel metadata, direct in-process subscription execution settings, and delayed publication settings from `Engine:Messaging` through `engine.AddEventingFromConfiguration(configuration)`. A host that wants a Wolverine-free managed subscription lane can configure channel and policy settings, but must declare subscription descriptors and execution code through typed code:

```json
{
  "Engine": {
    "Messaging": {
      "Channels": {
        "audit": {
          "DisplayName": "Audit",
          "Description": "Audit integration events.",
          "Tags": [ "audit", "configuration" ]
        }
      },
      "InProcessSubscriptions": {
        "EnableExecution": true,
        "MaxAttempts": 3,
        "RetryDelayMilliseconds": 250,
        "RetryBackoff": "exponential",
        "RetryBackoffMultiplier": 2,
        "RetryMaxDelayMilliseconds": 5000,
        "RetryJitterPercent": 15,
        "Idempotency": {
          "Enabled": true,
          "Store": "inbox",
          "RetentionMinutes": 45
        }
      }
    }
  }
}
```

The matching subscription belongs in code, for example through `options.Subscriptions.Add(...)` plus `services.AddCephalonEventSubscriptionExecutor<TExecutor>()`, through a reusable module that implements `IEventSubscriptionContributor`, by applying `EventSubscriptionAttribute` to the registered executor when the descriptor is static, or by letting the executor implement `IEventSubscriptionDescriptorProvider` when it owns richer descriptor metadata. Code-owned cross-cutting steps belong in DI too, through `services.AddCephalonEventSubscriptionExecutionMiddleware<TMiddleware>()` or direct `IEventSubscriptionExecutionMiddleware` registrations; the native publisher runs those middleware steps before the executor and surfaces `subscriptionExecutionPipeline` metadata for operators. `Engine:Messaging:Subscriptions` and `Engine:Messaging:SubscriptionHandlers` are rejected by `AddEventingFromConfiguration` so hot-path publish/subscribe behavior stays code-first; configuration remains for runtime policy such as retry, idempotency, routing, scheduling, and channel metadata.

That same in-process retry lane keeps `retryPolicy = bounded-in-process` and now also reports `retryBackoff`, `retryBackoffMultiplier`, `retryMaxDelayMilliseconds`, `retryJitterPercent`, and `retryEffectiveDelayMilliseconds` on `retry-scheduled` observations. `fixed` preserves the original delay behavior; `exponential` multiplies each failed attempt by the configured multiplier and caps it at the configured max delay; jitter is deterministic from publication, channel, event type, subscription, and attempt metadata so retries are shaped without introducing opaque randomness into operator evidence.

The same host can accept bounded process-local delayed publication requests without Wolverine by adding:

```json
{
  "Engine": {
    "Messaging": {
      "Publications": {
        "Scheduling": {
          "Enabled": true,
          "MaxDelayMilliseconds": 5000,
          "MaxPendingCount": 4
        }
      }
    }
  }
}
```

The same host can route publication requests by event type before the active publisher runs:

```json
{
  "Engine": {
    "Messaging": {
      "Publications": {
        "Routing": {
          "Enabled": true,
          "AutoChannelId": "auto",
          "RejectMismatchedExplicitChannel": true,
          "Routes": {
            "audit.created": "audit",
            "billing.*": "billing-events"
          }
        }
      }
    }
  }
}
```

Callers can then publish with `"channelId": "auto"` and let the engine resolve the effective channel. Operators can verify the active route policy through `eventing.publish`, `event-publishers`, publication runtime metadata, and the `routing-and-provider-portability` entry in `eventing-superiority-profile`.

The request still uses the existing event-publication command surface; callers add exactly one scheduling metadata key:

```json
{
  "metadata": {
    "scheduledForUtc": "2026-05-10T10:00:00.0000000+00:00"
  }
}
```

`delayMilliseconds` is the relative-delay alternative when the caller does not want to compute an absolute timestamp.

This scheduler is intentionally process-local. Pending delayed publications live in memory, are lost on process stop, are not coordinated across nodes, and do not replace a durable outbox dispatcher, broker scheduler, or provider-managed delayed-delivery lane.

The retry backoff and jitter knobs are intentionally process-local as well. They give lightweight Wolverine-free hosts configurable failure shaping and truthful retry metadata, but they do not create a durable retry queue, broker error policy, cross-node lease, or downstream delivery guarantee.

That still stops short of a full bus runtime on purpose. The core in-process execution path is direct, synchronous, and process-local for handler invocation, code-first middleware execution, and retry timing: inbox-backed idempotency can make completed-execution duplicate suppression durable through a registered `IInbox`, but it does not turn the lane into a broker, distributed dispatcher, durable retry queue, cross-node exactly-once delivery guarantee, or arbitrary background subscription poller. Native routing chooses or validates the effective Cephalon event channel before publication, but it does not create broker topology or claim partition/exchange/queue ownership. The publication runtime catalog is also bounded to the active publication path: direct in-process observations describe local subscription execution, while outbox-backed observations describe accepted staging and remain separate from later dispatch-state or broker-delivery truth. `eventing.subscribe` appears from the core pack only when `EnableInProcessSubscriptionExecution` is explicitly selected and a real executor exists; companion packs such as `Cephalon.Eventing.Wolverine` remain optional provider-managed ways to activate durable staged dispatch and richer retry behavior on top of the same runtime-neutral contracts. In all modes `IEventSubscriptionExecutionBindingCatalog` and `event-subscriptions` distinguish `application-managed`, `cephalon-managed`, and provider-managed runtime-bound ownership through `dispatchRuntime`, `subscriptionRuntime`, `executionRuntimeId`, `executionOwnership`, `executionMode`, `subscriptionExecutionPipeline`, and `binding.*` metadata while keeping the underlying core pack host-agnostic. Companion packs can continue adding more runtime truth under the same `event-driven-integration` technology when they have something honest to say. For example, the current `Cephalon.Data.EntityFramework`, `Cephalon.Data.MongoDB`, `Cephalon.Data.Redis`, `Cephalon.Data.Elasticsearch`, `Cephalon.Data.OpenSearch`, `Cephalon.Data.Neo4j`, `Cephalon.Data.Qdrant`, and `Cephalon.Data.Nats` slices can now project staged-only outbox producers and application-managed inbox stores into the same technology surface set, the subscription metadata can now say when such an inbox store is available, the native in-process lane can deliberately consume one such inbox for completed-execution idempotency, application-managed handlers can now report their latest runtime state and retry intent through `IEventSubscriptionRuntimeReporter` / `IEventSubscriptionRuntimeCatalog`, application-managed dispatch paths can now report outbox-specific dispatch observations through `IEventDispatchRuntimeReporter` / `IEventDispatchRuntimeCatalog`, configured dispatch runtimes now remain visible through `IEventDispatchRuntimeDescriptorCatalog` even before runtime reports arrive, the `IEventDispatchStore` contract can now bridge pending staged outbox rows plus durable dispatch outcomes across both relational and the current non-relational outbox packs without baking a specific broker into the pack, runtime descriptors now surface the canonical aggregate `Summary` instead of forcing each consumer to re-aggregate `IEventDispatchRuntimeCatalog` ad hoc, outbox descriptors now carry an effective dispatch policy instead of leaving `/engine/outboxes` as a static staged-only answer, terminal dispatch posture now appears directly as state/summary fields and `event-dispatches` / `event-dispatch-runtimes` metadata before consumers fall back to `reported.*`, `event-dispatches` still projects only the per-outbox detail that belongs to the current path, hosted executions can still link themselves to one or more declared subscriptions through metadata so the surface can report application-managed execution linkage, execution-graph linkage, and live hosted-execution/runtime-story phase data without pretending that delivery, durable retries, handlers, or distributed subscription execution are already owned by the eventing pack, the current `Cephalon.Eventing.Behaviors` slice can now explicitly route `ISagaChoreographyPublisher` traffic into the same publication path without moving choreography ownership into `Cephalon.Eventing` itself, and the current `Cephalon.Eventing.Wolverine` slice can still opt into both a `wolverine-managed` staged-event dispatch loop and runtime-bound subscription execution proof on top of the same runtime-neutral contracts.

ClickHouse is now an explicit unsupported-by-design branch instead of a hidden parity gap: `Cephalon.Data.ClickHouse` still stages durable outbox records, but it now publishes `DispatchPolicy.PolicyId = unsupported` plus `ExecutionMode = disabled` so `/engine/outboxes`, `event-dispatches`, and runtime surfaces say clearly that the current analytics-first replacement semantics are staging-only and do not yet own mutable dispatch state. `Cephalon.Data.Cassandra` now takes the opposite path through a Cassandra-native sharded pending-dispatch index, so wide-column workloads can stay on the same consumer-managed or adapter-managed dispatch path without relaying through a relational bridge.

## Adapter stance

Current Cephalon direction for messaging adapters:

- `Cephalon.Eventing` now also has an opt-in `cephalon-managed` direct in-process subscription execution baseline, with config-driven channel descriptor discovery, code-first subscription descriptors, descriptor-bearing executors, and execution middleware, optional bounded process-local retries using fixed/exponential backoff plus deterministic jitter metadata, duplicate-completed execution suppression backed either by process-local memory or by exactly one registered `IInbox`, config-driven publication routing, and bounded process-local scheduled publication acceptance, for lightweight hosts that do not need a durable broker or Wolverine
- `Cephalon.Eventing.Wolverine` is the current shipped optional companion adapter proof and now supports host wiring, an opt-in `wolverine-managed` durable dispatch loop on top of `IEventDispatchStore`, bounded dispatch retry with terminal failed state, and a provider-managed runtime-bound subscription execution lane on top of the same staged-publication path
- `MassTransit` and `NServiceBus` are reference benchmarks for transport topology, routing, saga, recoverability, outbox, and operator tooling semantics; they should not become shipped adapter dependencies unless a future slice explicitly targets interop or migration compatibility
- other messaging libraries such as `MediatR`, `LiteBus`, `NServiceBus`, and `SlimMessageBus` are allowed in consumer apps, but they are consumer-owned integrations rather than shipped Cephalon companion adapters in the current phase
- Cephalon should not claim runtime truth for those consumer-owned integrations unless a dedicated bridge or adapter package actually projects that state back into the engine surfaces
- engine readiness should come from `Cephalon.Eventing` owning the runtime-neutral contracts and truthful operator surfaces; companion adapters are optional ways for a consumer app to activate one managed runtime on top of those seams
- one message flow should have one durable-messaging owner; avoid mixing Cephalon/Wolverine-managed outbox, inbox, retry, or dispatch semantics with a second durable bus/runtime for the same flow
- adapter-owned diagnostics conventions should be projected through `/engine/diagnostics` when a shipped companion adapter emits stable event ids, which the current Wolverine slice now does for its managed dispatch loop

## Related docs

- [Cephalon.Eventing.Behaviors](eventing-behaviors.md)
- [Technology packs](../technology-packs.md)
- [Module authoring](../module-authoring.md)
