# Engine Completion Scorecard

This scorecard is the roll-up view for answering one question: "what still blocks Cephalon from being complete enough to publish and support as a reusable engine platform?"

It is not a new source of package truth. It reads the existing truth sources and turns them into a product-readiness checklist for preview, release-candidate, and eventual GA decisions.

Cross-references: [`project-memory.md`](project-memory.md), [`engineering-standards.md`](engineering-standards.md), [`engine-surface-maturity-audit.md`](engine-surface-maturity-audit.md), [`conformance-matrix.md`](conformance-matrix.md), [`runtime-contract-index.md`](runtime-contract-index.md), [`compatibility.md`](compatibility.md), [`deployment-mode-support.md`](deployment-mode-support.md), [`dotnet11-readiness.md`](dotnet11-readiness.md), [`package-publishing.md`](package-publishing.md), [`supply-chain-uplift-plan.md`](supply-chain-uplift-plan.md), [`sre-posture.md`](sre-posture.md), [`test-coverage-roadmap.md`](test-coverage-roadmap.md), [`release-checklist.md`](release-checklist.md), [`engine-roadmap.md`](engine-roadmap.md), [`engine-backlog.md`](engine-backlog.md), [`planning-governance.md`](planning-governance.md).

## Role

Use this page before any release train, architecture review, NuGet publication decision, or large planning reset.

The scorecard exists to prevent three common failure modes:

- a package reads as production-ready because it has a high maturity label, even though release, deployment, package, SRE, or support evidence is still missing
- a readiness lane such as `.NET 11`, trimming, Native AOT, or single-file starts reading like a supported baseline before the repo has proof
- planning cards advance one quality dimension while silently regressing another, such as improving usability by weakening compatibility or improving performance by reducing auditability

## Status vocabulary

Use these values when summarizing a gate or package family.

| Status | Meaning |
| --- | --- |
| `ready-for-preview` | Enough evidence exists for preview adopters, with documented gaps and no unsupported claim. |
| `partial` | Useful evidence exists, but one or more required proof sources are still missing. |
| `blocked` | A known gap prevents the gate from being promoted. |
| `not-claimed` | Cephalon deliberately makes no support promise yet. This is valid when the docs say so clearly. |
| `not-applicable` | The gate does not apply to the package or release shape. |
| `needs-refresh` | The evidence source is date-sensitive or known to drift and must be refreshed before release. |

Do not use `M4`, "tests pass", or "docs exist" as a synonym for GA. Maturity, validation, packaging, operations, support, and adoption are separate dimensions.

## Evidence sources

| Evidence source | Truth owned there | How the scorecard consumes it |
| --- | --- | --- |
| [`engine-surface-maturity-audit.md`](engine-surface-maturity-audit.md) | Package maturity, ownership mode, next proof needed | Reads current `M0`-`M4` posture; never overrides it. |
| [`conformance-matrix.md`](conformance-matrix.md) | Per-package adoption truth, routes, snapshot keys, catalog interfaces | Checks whether adopters can find the runtime answer for each package family. |
| [`runtime-contract-index.md`](runtime-contract-index.md) | `/engine/*`, `snapshot.*`, and runtime catalog contract map | Checks operator/API contract completeness. |
| [`compatibility.md`](compatibility.md) | Framework, package, manifest, generator, and support-claim alignment rules | Checks whether a change can ship without silent drift. |
| [`deployment-mode-support.md`](deployment-mode-support.md) + `scripts/deployment-mode-support.json` + generated `claim-validation-report.json` | trim, Native AOT, single-file, publish-probe support posture, report-backed gate truth, and manifest-backed hazard inventory | Controls deployment-mode promotion; `claim-truthful` is required before support claims widen, `PublishProbePolicy` explains which mode release validation publishes by default, `PublishProbeGate` records whether the gated probe passed, `DeploymentModeEvidence.ClaimsReport*` validates the generated report's gate/target/warning/error/package-claim counts, and `hazard-inventory.json` makes tier/scoped-claim/transitive-hazard counts, dynamic route boundary annotation audit status, core/full-common/full-operator request-delegate proof, operator response JSON contract proof, Cephalon-owned non-operator endpoint proof, framework endpoint boundary proof, plus the lock-file-backed transitive audit visible in validation artifacts. |
| [`dotnet11-readiness.md`](dotnet11-readiness.md) | Future SDK assessment and current framework baseline | Keeps `net10.0` shipping truth separate from `.NET 11` readiness. |
| [`package-publishing.md`](package-publishing.md) + [`supply-chain-uplift-plan.md`](supply-chain-uplift-plan.md) + [`../scripts/supply-chain-release-support.json`](../scripts/supply-chain-release-support.json) + [`../scripts/validate-package-metadata.ps1`](../scripts/validate-package-metadata.ps1) + [`../scripts/validate-nuget-vulnerability-audit.ps1`](../scripts/validate-nuget-vulnerability-audit.ps1) + [`../scripts/validate-supply-chain-external-policy-preflight.ps1`](../scripts/validate-supply-chain-external-policy-preflight.ps1) + [`../scripts/invoke-signed-release-dry-run.ps1`](../scripts/invoke-signed-release-dry-run.ps1) + [`.github/workflows/publish-release.yml`](../.github/workflows/publish-release.yml) | NuGet/package artifact, package metadata/readme/tag/source/symbol validation, vulnerability-audit posture, provenance, SBOM, signing, trusted-publishing posture | Checks release evidence and supply-chain readiness, including which package metadata, symbol-pairing, and NuGet vulnerability-audit checks are archive-validated, which provenance items are workflow-ready, which nuget.org-side policies remain external prerequisites, which fail-closed preflight checks must pass before a real tag can log in to NuGet, whether an Actions-enabled release-manager identity has created the signed-release dry-run workflow run, which JSON report and handoff artifact carry that proof, and which dispatch actor/action is blocking that proof when it has not. |
| [`sre-posture.md`](sre-posture.md) + [`benchmarking.md`](benchmarking.md) + [`../scripts/sre-posture-support.json`](../scripts/sre-posture-support.json) + [`../scripts/sre-stable-baselines.json`](../scripts/sre-stable-baselines.json) + [`../scripts/measure-ci-flake-rate.ps1`](../scripts/measure-ci-flake-rate.ps1) | Engine SLIs, SLOs, benchmark guardrails, stable-baseline artifact, CI flake-rate evidence, error-budget rules | Checks operational reliability and performance evidence, including which SLI targets are declared, which SLIs are mapped to benchmark guardrail entries, which benchmark-backed baselines are published, which baselines are still pending, and whether pending CI flake-rate evidence is blocked by missing run history versus missing Actions/workflow readiness. |
| [`test-coverage-roadmap.md`](test-coverage-roadmap.md) | Layered test posture, gap-definition criteria, prioritized recommendations, and test-flake quarantine queue | Emits `TestCoverageEvidence` so release readback can fail closed when the roadmap still has active recommendation gaps or open quarantine work. |
| [`release-checklist.md`](release-checklist.md) | Release-manager proof sequence | Converts the scorecard into per-release execution. |
| [`../scripts/adoption-smoke-support.json`](../scripts/adoption-smoke-support.json) + [`../scripts/validate-generated-app-adoption.ps1`](../scripts/validate-generated-app-adoption.ps1) + [`../scripts/validate-template-pack-adoption.ps1`](../scripts/validate-template-pack-adoption.ps1) + [`../scripts/validate-modular-monolith-adoption.ps1`](../scripts/validate-modular-monolith-adoption.ps1) + [`../scripts/validate-vertical-slice-eventing-adoption.ps1`](../scripts/validate-vertical-slice-eventing-adoption.ps1) + [`../scripts/validate-microservice-multi-transport-adoption.ps1`](../scripts/validate-microservice-multi-transport-adoption.ps1) + [`../scripts/validate-saas-tenant-governance-audit-adoption.ps1`](../scripts/validate-saas-tenant-governance-audit-adoption.ps1) + [`../scripts/validate-out-of-tree-package-adoption.ps1`](../scripts/validate-out-of-tree-package-adoption.ps1) | Out-of-repo generated-app, template-pack, modular-monolith REST/Worker/data, vertical-slice Eventing/outbox, microservice REST/JSON-RPC/gRPC multi-transport, SaaS tenant governance/invitation/audit, local-package, reference-module staging, host-run, runtime-surface adoption replay, execution-report contract, and golden use-case map | Checks that the developer-adoption gate points at replayable external smoke paths, machine-readable execution reports, and a concrete set of real app shapes instead of prose-only adoption confidence. |
| [`../scripts/provider-integration-support.json`](../scripts/provider-integration-support.json) + [`../scripts/observability-dependency-health-providers.json`](../scripts/observability-dependency-health-providers.json) + [`../scripts/run-provider-live-testcontainers.ps1`](../scripts/run-provider-live-testcontainers.ps1) + [`../.github/workflows/provider-live-testcontainers.yml`](../.github/workflows/provider-live-testcontainers.yml) | Provider live-proof, external-service-gate, runtime-contract, dependency-health managed-probe runtime proof, and Docker-capable live-execution evidence | Checks whether provider claims have machine-readable proof rows before release readback; repo-owned disposable live lanes can run by default, externally managed live provider lanes remain opt-in, dependency-health rows prove the managed probe loop across all eighteen provider packs without external services, dependency-health provider family shape plus provider-integration rows are guarded by a source-derived manifest, and the heavy Testcontainers provider proof has a scheduled/manual CI lane outside default release validation. |
| [`../scripts/eventing-operational-superiority-support.json`](../scripts/eventing-operational-superiority-support.json) + [`components/eventing.md`](components/eventing.md) + [`project-memory.md`](project-memory.md) + [`../src/Cephalon.Eventing/Services/EventingSuperiorityProfileRuntimeSurfaceContributor.cs`](../src/Cephalon.Eventing/Services/EventingSuperiorityProfileRuntimeSurfaceContributor.cs) | Native Eventing operational-superiority promotion contract, required dimension coverage, code-first hot-path posture, provider neutrality, Wolverine optionality, and runtime-source concordance | Emits `EventingOperationalSuperiorityEvidence` so release validation and `cephalon doctor --scorecard` can fail closed when the native Eventing operational-superiority contract drifts, when any required dimension stops being `claimed`, when hot-path publish/subscription stops being code-first, when Wolverine becomes required, or when the support manifest no longer matches the runtime profile source. |
| [`../scripts/summarise-public-api-deltas.ps1`](../scripts/summarise-public-api-deltas.ps1) + `src/Cephalon.*/PublicAPI.*.txt` | Per-package public API delta files, shipped baselines, additive entries, removal entries, Markdown/JSON delta artifacts, and the release-validation removal gate | Checks that the public API compatibility gate has machine-readable package counts and removal counts instead of prose-only review posture; release validation runs the delta summary with `-JsonOutputPath` and `-FailOnRemovals`. |
| [`../scripts/publish-surface-maturity-report.ps1`](../scripts/publish-surface-maturity-report.ps1) + [`engine-surface-maturity-audit.md`](engine-surface-maturity-audit.md) + [`conformance-matrix.md`](conformance-matrix.md) + [`components/README.md`](components/README.md) | Source-project, component-doc, maturity, ownership, conformance, provider-manifest, and planning-baseline reconciliation | Emits a separate Gate 0 artifact and fails release validation before the scorecard is generated when package maturity truth drifts. |

## Machine-readable report

[`scripts/publish-engine-completion-scorecard.ps1`](../scripts/publish-engine-completion-scorecard.ps1) exports this page into a release artifact without becoming a new authority. The script parses the status vocabulary, evidence sources, platform gates, quality dimensions, and package-family roll-up from this Markdown document, validates every scorecard status token against the declared vocabulary, validates every repo-local evidence-source reference, reads per-package GA readiness rows from [`conformance-matrix.md`](conformance-matrix.md), validates the deployment-mode support manifest and the generated deployment-mode claims report before emitting `DeploymentModeEvidence`, validates the adoption-smoke support manifest, its execution-report contract, and its golden use-case map before emitting `AdoptionSmokeEvidence`, validates the provider-integration support manifest before emitting `ProviderIntegrationEvidence`, validates dependency-health provider rows against `dependencyHealthProviderManifest` / [`observability-dependency-health-providers.json`](../scripts/observability-dependency-health-providers.json), validates [`eventing-operational-superiority-support.json`](../scripts/eventing-operational-superiority-support.json) plus runtime concordance tokens in [`EventingSuperiorityProfileRuntimeSurfaceContributor.cs`](../src/Cephalon.Eventing/Services/EventingSuperiorityProfileRuntimeSurfaceContributor.cs) before emitting `EventingOperationalSuperiorityEvidence`, validates the SRE posture support manifest and stable-baseline manifest before emitting `SrePostureEvidence`, validates the supply-chain release support manifest, NuGet vulnerability-audit workflow evidence, external-policy preflight contract, signed-release dry-run probe script/readback/handoff contract, and tag-triggered release workflow before emitting `SupplyChainEvidence`, validates the test-coverage roadmap layered project links, gap criteria, recommendation posture, and quarantine queue before emitting `TestCoverageEvidence`, and scans `src/Cephalon.*/PublicAPI.Unshipped.txt` plus matching shipped baselines before emitting `PublicApiCompatibilityEvidence`.

- `artifacts/engine-completion-scorecard-release/engine-completion-scorecard.json`
- `artifacts/engine-completion-scorecard-release/README.md`

Release validation first emits `artifacts/surface-maturity-report-release/surface-maturity-report.json`. That artifact is a fail-closed prerequisite for the scorecard, not a new maturity authority or a duplicate scorecard schema. The GitHub Actions release-validation workflow must keep `actions/setup-dotnet` cache dependency matching the repository's project-level `**/packages.lock.json` layout so setup succeeds before fail-closed artifacts are expected, and the hosting/tooling validation lane must keep cross-platform reference-doc path-separator normalization, generated deployment-script doctor audit normalization, checked-in generated-reference parity, behavior resilience timeout determinism, CDC checkpoint preservation, showcase read-model projection-loop determinism, and root-provider DI validation for sample hosts from drifting. Promoted wall-time SRE baseline rows still have to be passed timings below target; current-run wall-time overruns remain visible through matching `investigate` timing reports and do not prevent later release-validation evidence artifacts from being published.

The JSON artifact uses schema version `1.25.0`. Release validation publishes it by default through `scripts/validate-release.ps1` unless `-SkipEngineCompletionScorecard` is passed for a deliberately narrower local run. The release-validation scorecard step also prints deployment-mode global-claim, package-scoped-claim, known-hazard, transitive-audit, publish-probe mode, claims-report gate, target, warning, error, package-claim verdict counts, boundary-annotation audit status/failures, boundary/core/full-common/full-operator route-delegate audit status/failures, operator response JSON contract audit status/failures, non-operator endpoint audit status/failures, and framework endpoint boundary audit status/failures beside adoption-smoke scenario/runtime-probe/assertion/execution-report-field counts, golden use-case count, execution-ready golden use-case count, golden use-case execution-report count, provider integration row/live/composition/external-gate/default-skip/runtime-contract counts plus dependency-health provider-manifest reference/schema/status/provider-count readback, Eventing operational-superiority contract/status/required-dimension/coverage/promotion/hot-path/Wolverine-optional/runtime-concordance readback from `EventingOperationalSuperiorityEvidence`, SRE SLI count, target-declared count, pending stable-baseline count, stable-baseline count, stable-baseline manifest/row/measurement counts, SRE pending-baseline row/blocker/evidence counts, SRE guardrail-mapped/pending/not-applicable coverage counts, supply-chain workflow/external-policy/preflight counts, preflight status, and signed-release dry-run status/proof/blocker/scope/diagnostic/identity/action/required-command/output/handoff readback, test coverage layered-project/gap-criterion/recommendation/quarantine counts from `TestCoverageEvidence`, and public API package/pending/addition/removal counts from the generated artifact. The supply-chain readback now includes the workflow-ready NuGet vulnerability audit evidence item and the signed-release dry-run proof posture, while the default release-validation step itself writes the advisory report and fails closed if any advisory is found. The script-level Pester suite now dot-sources release validation with `CEPHALON_VALIDATE_RELEASE_NO_RUN` and proves the adoption-smoke execution-report readback, dependency-health provider-manifest readback, Eventing operational-superiority promotion and runtime-concordance readback, SRE stable-baseline and pending-baseline manifest readback, supply-chain preflight and signed-release dry-run readback, test-coverage evidence readback, and deployment-mode claims-report readback print from the generated JSON shape and fail when required generated nodes are missing or drifted. It then fails closed on scorecard hard blockers: blocked platform gates, supply-chain blocked evidence items, public API removal entries, active test-coverage gaps, open test-flake quarantine entries, blocked Eventing operational-superiority promotion, Wolverine-required Eventing promotion evidence, drifted Eventing runtime concordance, or an invalid deployment-mode claims-report gate, boundary-annotation audit, core route-delegate audit, full-common route-delegate audit, full-operator route-delegate audit, operator response JSON contract audit, non-operator endpoint audit readback, or framework endpoint boundary audit readback. The later public-API delta summary step writes `artifacts/public-api-delta-release/public-api-delta.md` plus `artifacts/public-api-delta-release/public-api-delta.json` and passes `-FailOnRemovals`, so removal entries still leave reviewable Markdown and machine-readable JSON artifacts before they fail the release run.

For a local release-manager readback, `cephalon doctor --scorecard <path-to-engine-completion-scorecard.json>` summarizes that generated artifact's schema/source documents, platform-gate posture, validated evidence-reference count, per-package GA-readiness count, deployment-mode counts from `DeploymentModeEvidence` (global claim rows, global `not-claimed` rows, package-scoped claim packages, known hazard packages/entries, transitive audit entries, publish-probe mode, claims-report path, gate status, target count, warning count, error count, and package-claim verdict counts, plus boundary/core/full-common/full-operator route-delegate, operator response JSON contract, non-operator endpoint, and framework endpoint boundary audit status/failure counts), adoption-smoke counts from `AdoptionSmokeEvidence` (scenario count, runtime probes, assertions, execution-report required fields with execution-report default path/schema, golden use cases, execution-ready golden use cases, and golden use-case execution reports), provider integration counts from `ProviderIntegrationEvidence` (evidence rows, live proofs, composition-only rows, external-service gates, default-skipped rows, runtime contracts, and the `DependencyHealthProviderManifest` reference/schema/status/provider count), Eventing operational-superiority counts from `EventingOperationalSuperiorityEvidence` (promotion contract, target, status, required status, required/covered/partial/missing dimensions, coverage percentage, promotion gate/decision, code-first hot-path binding mode, Wolverine-required flag, runtime-concordance status, runtime source, and matched token counts), SRE posture counts from `SrePostureEvidence` (SLIs, target-declared SLIs, pending stable baselines, stable baselines, stable-baseline manifest rows/measurements, pending-baseline blocker evidence rows, and guardrail coverage counts), supply-chain release evidence counts from `SupplyChainEvidence` (evidence items, workflow-ready items, external-policy-pending items, external-policy preflight checks/status, signed-release dry-run status/proof/blocker/scope/diagnostic/output/handoff, and blocked items), test coverage counts from `TestCoverageEvidence` (layered projects, gap criteria, recommendations by shipped/gated/active status, quarantine entries, open quarantine entries, and queue status), and public API compatibility counts from `PublicApiCompatibilityEvidence` (package baselines, pending packages, additions, and removals). The CLI consumes the generated JSON read model only; it does not parse this Markdown page, replace source documents, or promote support/GA claims by itself.

## Platform-level gates

The September [completion plan](framework-completion-plan.md) maps unresolved cross-cutting gates to ENG-727–738 and operator gates to ENG-719–726. This planning refresh changes no gate to ready and no package to M4/GA. ENG-532 remains externally blocked pending a fresh successful hosted proof.

These gates must all be `ready-for-preview`, `not-applicable`, or explicitly documented as `not-claimed` before a preview release. For a GA release, no gate can remain `blocked`, and any `partial` gate must have a release-manager-approved exception.

| Gate | Current posture | Blocks preview? | Blocks GA? | Next proof needed |
| --- | --- | --- | --- | --- |
| Product identity and architecture contract | `ready-for-preview` | No | No, while docs/source/planning stay aligned | Keep `project-memory.md`, `architecture.md`, and `long-range-direction.md` aligned when the engine shape changes. |
| Package maturity and ownership truth | `ready-for-preview`; the Gate 0 report reconciles `107` source projects, `107` component docs, `89` exact conformance rows, and `18` provider-manifest rows with zero drift | No | Yes, if the report, audit, component docs, provider manifest, or matrix drifts | Keep every shipped package row current and keep `publish-surface-maturity-report.ps1 -FailOnDrift` wired into release validation. |
| Runtime introspection contract | `ready-for-preview` | No | Yes, if routes or snapshot keys drift | Keep `/engine/*`, `snapshot.*`, and catalog interfaces in the runtime contract index. |
| Public API compatibility | `partial`; the Gate 1 and bounded coordination contracts are recorded as a reviewable next-release delta, so the generated readback is `104` baselines / `3` pending packages / `282` additions / `0` removals, and removal entries remain a default release-validation failure through `scripts/summarise-public-api-deltas.ps1 -JsonOutputPath <path> -FailOnRemovals`; stable package-validation baselines remain future GA work | No | Yes | Review the `Cephalon.Abstractions`, `Cephalon.AspNetCore` and `Cephalon.Engine` additions before their release-tag promotion, keep generated `PublicApiCompatibilityEvidence` aligned with the baseline files, and enable package validation baselines when stable packages ship. |
| Package publishing and NuGet discoverability | `partial`; produced packages now validate readmes, tags, license, repository/source metadata, package type, and runtime/tool `.snupkg` pairing through `scripts/validate-package-metadata.ps1` / `package-metadata-validation.json`; `scripts/invoke-signed-release-dry-run.ps1` records signed-release dry-run readiness, `SupplyChainEvidence.SignedReleaseDryRun` now reads back the same status/proof/blocker/scope/diagnostic/identity/action/command/output/handoff contract, and the May 15, 2026 probe reports repository/workflow dispatch ready with blocker scope identity and GitHub blocker class dispatch-identity-actions-disabled | No | Yes | Keep archive-level package metadata validation green, rerun the signed-release dry-run probe from an Actions-enabled release-manager identity until it records a workflow run URL, and reserve/protect `Cephalon.*` when eligible. |
| Supply-chain and release provenance | `partial` | No | Yes | Keep generated `SupplyChainEvidence` aligned with the tag-triggered release workflow, fail-closed NuGet vulnerability audit, and fail-closed external-policy preflight; complete the nuget.org-side trusted-publishing policy, `Cephalon.*` prefix reservation, `NUGET_USER` secret, and protected release-manager confirmation variables before any real release tag push. |
| Deployment-mode support claims | global `not-claimed` for trim, Native AOT, and single-file support; `Cephalon.Diagnostics`, `Cephalon.Abstractions`, and `Cephalon.Scaffolding` have package-scoped single-file claims; `Cephalon.Data.MySql.SciSharpReplication` and `Cephalon.ReferenceDocs` are deliberate package-level `not-claimed` boundaries; bounded `Cephalon.Engine` trim blockers have been reduced to dynamic package loading while Native AOT still also has behavior-input JSON materialization work; harness emits `PublishProbePolicy` plus `PublishProbeGate` showing the representative `singleFile` publish probe is release-blocking, while trim and Native AOT remain audit-only; HazardInventory / `hazard-inventory.json` carries the matched transitive-hazard lock-file audit subset, matched ASP.NET Core dynamic route boundary annotation audit, core/full-common/full-operator request-delegate proof, operator response JSON contract proof, Cephalon-owned non-operator endpoint proof, and framework endpoint boundary proof, and the harness fails closed if boundary annotation, route-delegate, response-contract, non-operator endpoint, or framework endpoint boundary drift returns | No, because the lack of global claim is explicit, the scoped claims are bounded, the gated probe is narrower than a support claim, the operator/non-operator proofs are bounded to Cephalon-owned route surfaces, the framework-owned health/OpenAPI/Scalar endpoint extensions remain an explicit adapter support boundary, and the permanent non-claims are documented | Yes, if GA claims those modes globally or includes those packages in a promoted deployment-mode set | Promote support only after manifest, project properties, structural remediation or explicit exclusion, workflow, docs, framework endpoint extension posture, and package guidance agree; keep the single-file publish gate passing before every release. |
| `.NET 11` migration | `partial`; September 16, 2026 source refresh observes .NET 11 RC1 / SDK 11.0.100-rc.1 (September 8), while repository SDK 10.0.303 and net10.0 remain shipping truth; RC1 validation is pending ENG-731 | No | Not for a net10.0 GA; yes for any .NET 11 support claim | Assess current servicing and RC compatibility, retain the separate readiness lane, and update all support surfaces only after passing evidence. |
| Engine SRE posture | `partial` | No | Yes | Keep generated `SrePostureEvidence` aligned with declared SLI targets, benchmark guardrail coverage references, stable-baseline manifest, pending-baseline blocker rows, release-validation readback, and source docs; the current stable subset covers four benchmark-backed hot-path SLIs, the ASP.NET Core core-operator-surface cold-start mean-baseline proxy, the Worker cold-start mean-baseline proxy, one deployment-mode claims-report SLI, plus restore locked-mode, reference-docs, and full validate-release wall-time SLIs, while flake-rate remains `pending-stable-baseline` with collector-backed `ci-metadata-unavailable` blocker evidence until live GitHub Actions run history exists. |
| Performance guardrails | `partial` | No | Yes | Keep benchmark smoke and guardrail validation current for hot paths; tighten SLOs after stable baselines. |
| Security and compliance posture | `partial` | No | Yes | Keep trust policy, fail-closed NuGet vulnerability-audit reports, signing, audit metadata, and compliance evidence in the release bundle. |
| Developer adoption and low-code ergonomics | `partial` | No | Yes | Prove external generated-app, template-pack, CLI, package-stage, and reference-module flows outside the repo. |

## Quality-dimension gates

Every meaningful `ENG-*` card should name which quality dimensions it advances. A release is not "complete" just because many cards shipped; it is complete when the evidence below exists for the release scope.

| Quality | Release-ready evidence |
| --- | --- |
| Performance | Benchmark smoke passes, guardrails show no unapproved regression, and hot-path SLOs are not burning. |
| Security | Trust, signature, vulnerability, secret-handling, and package-policy evidence are current. |
| Usability | CLI, templates, generated apps, samples, docs, and IntelliSense tell one coherent story with minimal consumer code. |
| Reliability | Failure policy, retries, timeouts, circuit breakers, health, and runtime-story answers are explicit and tested. |
| Maintainability | Source, hand-authored docs, generated reference docs, roadmap, backlog, and project tracking agree. |
| Scalability | Cell, edge, topology, provider, and distributed-execution claims are explicit about ownership and limits. |
| Flexibility | Architecture, transport, host, topology, and policy changes are expressed through engine configuration/composition where feasible. |
| Compatibility | Binary/API baselines, package dependencies, framework baselines, and manifest contracts are reviewed before release. |
| Data integrity | Outbox/inbox, CDC, event-sourcing, idempotency, durable execution, and replay semantics have focused proof. |
| Availability | Liveness/readiness, dependency-health, degradation posture, and deployment-path evidence are visible. |
| Auditability | Runtime catalogs, diagnostics, package provenance, governance decisions, and release artifacts are inspectable. |
| Compliance | SBOM/provenance, CRA/AI/security framework mappings, data-handling posture, and vulnerability-response evidence are collected. |

## Package-family readiness roll-up

This table is intentionally family-level. The per-package truth remains in the maturity audit and conformance matrix.

| Family | Current scorecard posture | GA blockers to keep visible |
| --- | --- | --- |
| Core runtime (`Abstractions`, `Engine`, ASP.NET Core, Worker) | `ready-for-preview` with shipped public API baselines and deployment-mode gaps still tracked | Package validation baselines, release SLI baselines, deployment-mode claim truth. |
| Transports and REST projection | `ready-for-preview` for the shipped runtime contracts | Keep OpenAPI/Scalar/runtime route truth aligned with behavior REST governance and non-REST boundaries. |
| Behaviors and resilience | `ready-for-preview` for the shipped managed lanes | Keep benchmark guardrails and runtime diagnostics current before widening hot-path claims. |
| Data, eventing, event-sourcing, CDC | `partial` because many provider lanes exist with different ownership levels | Do not collapse provider-managed, application-managed, and cephalon-managed ownership; keep durable/distributed claims explicit and keep `ProviderIntegrationEvidence` live-proof posture aligned with the actual test lanes. |
| Agentics and retrieval | `partial` with first managed/operator lanes shipped | Keep AI/provider orchestration, durable memory, vector/embedding, and autonomous planning outside the claim until a package owns them. |
| Multi-tenancy governance | `partial` with many bounded governance proofs shipped | Provider-backed distributed storage, identity-provider sync, public onboarding, and UI/backoffice remain future package-owned work. |
| Edge, cell, and topology automation | `partial` with strong runtime truth and provider proofs | Keep multi-provider mutation, teardown, and distributed control-plane claims scoped to shipped provider packages. |
| Observability, diagnostics, and cloud/provider packs | `ready-for-preview` for current companion posture | Keep SLI emission, event-id registry, the dependency-health provider manifest, provider docs, and operational validation aligned. |
| Tooling, CLI, scaffolding, templates, reference docs | `partial` | GA needs out-of-repo adoption smoke, release artifact proof, package-stage proof, hosted-reference docs proof, and clear support boundaries. |

## Per-package GA readiness read model

The generated report includes `PackageGAReadiness` rows for every package row in [`conformance-matrix.md`](conformance-matrix.md). These rows intentionally do not copy the matrix table into this page. They carry the package name, family, maturity, ownership, GA gate status, blocker class, and source-document references so release managers can sort the JSON artifact without treating this scorecard as a maturity authority.

The mapping is conservative:

- `M4` rows remain `partial` for GA until release, SRE, package-validation, supply-chain, public API, and deployment-mode evidence pass for the release scope
- `M3`, `M2`, and `M1` rows remain `partial` until their next maturity proof and release evidence are complete
- `M0` or `taxonomy-only` rows are `not-claimed` for runtime support unless the owning docs deliberately widen the claim

## Promotion rules

1. A package or gate can move upward only when the owning evidence source moves first.
2. A support claim can never be promoted from this page alone.
3. A `not-claimed` posture is acceptable only when the public docs explain the non-claim clearly.
4. A high maturity label does not bypass release validation, package publishing, SRE, supply-chain, or deployment-mode evidence.
5. `.NET 11` preview readiness can inform migration planning, but it does not change the `net10.0` shipping floor until the migration lane is approved across source, docs, templates, package metadata, and planning.
6. If a gate is date-sensitive, refresh the source document first, then update this scorecard in the same slice.

## First follow-through queue

The next completion-oriented slices should stay narrow and evidence-driven:

1. Keep the shipped machine-readable scorecard emitter source-reference validation and per-package readiness mapping aligned with source docs instead of duplicating truth by hand.
2. Keep per-package GA readiness rows generated from maturity-audit and conformance-matrix truth; do not hand-maintain a second package table here.
3. Keep the clean-baseline package-scoped claims (`Cephalon.Diagnostics`, `Cephalon.Abstractions`, and `Cephalon.Scaffolding` single-file), the two deliberate high-tier package non-claims (`Cephalon.Data.MySql.SciSharpReplication` and `Cephalon.ReferenceDocs`), the emitted publish-probe policy/gate, and the emitted hazard inventory honest across manifest, project properties, harness output, workflow, docs, and package guidance while global deployment-mode rows remain `not-claimed`.
4. Keep `cephalon doctor --scorecard <path>` aligned with the generated JSON schema, including `DeploymentModeEvidence`, `AdoptionSmokeEvidence.ExecutionReport`, `ProviderIntegrationEvidence.DependencyHealthProviderManifest`, `EventingOperationalSuperiorityEvidence`, `SrePostureEvidence`, `SupplyChainEvidence`, `TestCoverageEvidence`, and `PublicApiCompatibilityEvidence`, so local summaries remain a readback of the scorecard artifact, not a second source of truth.
5. Keep the out-of-repo generated-app adoption smoke evidence manifest aligned with `validate-out-of-tree-package-adoption.ps1` so the generated scorecard artifact keeps naming the local-package, reference-module staging, host-run, runtime operator-surface probes, and default execution-report path/schema/required fields it validates.
6. Keep public API delta publishing aligned with `scripts/summarise-public-api-deltas.ps1 -JsonOutputPath <path> -FailOnRemovals` and the per-package `PublicAPI.*.txt` files so the generated artifact, public-API JSON artifact, Markdown report, and CLI readback keep reporting the same package/addition/removal counts while release validation fails closed on removals.
7. Keep SRE posture evidence aligned with `scripts/sre-posture-support.json`, `scripts/sre-stable-baselines.json`, `scripts/measure-ci-flake-rate.ps1`, `docs/sre-posture.md`, `docs/benchmarking.md`, benchmark guardrails, deployment-mode claims-report readback, and release-validation readback so the four hot-path benchmark baselines, ASP.NET Core core-operator-surface cold-start mean-baseline proxy, Worker cold-start mean-baseline proxy, deployment-mode claim-truth baseline, restore/reference-docs/full validate-release wall-time baselines, Actions/workflow readiness evidence, and cold-start guardrail mappings stay machine-checkable while flake-rate remains pending until live CI run history exists.
8. Keep supply-chain release evidence aligned with `scripts/supply-chain-release-support.json`, `scripts/validate-package-metadata.ps1`, `scripts/validate-nuget-vulnerability-audit.ps1`, `scripts/validate-supply-chain-external-policy-preflight.ps1`, `scripts/invoke-signed-release-dry-run.ps1`, `.github/workflows/publish-release.yml`, `docs/package-publishing.md`, and `docs/supply-chain-uplift-plan.md` so archive-level package discoverability, fail-closed vulnerability audit, workflow-ready provenance, pending external publisher policy, signed-release dry-run dispatch proof/readback/handoff, and the fail-closed preflight needed for real tags stay visible together.
9. Keep provider integration evidence aligned with `scripts/provider-integration-support.json`, `scripts/observability-dependency-health-providers.json`, `scripts/run-provider-live-testcontainers.ps1`, `.github/workflows/provider-live-testcontainers.yml`, provider live-test gates, data provider runtime-surface tests, SMTP invitation-delivery relay proof, CDC integration lanes, and dependency-health managed-probe runtime proofs so "works with every provider" remains machine-checkable instead of aspirational prose. The non-relational provider live lanes and SMTP relay lane must continue documenting both supported runtime modes where applicable: pre-provisioned services and the explicit `CEPHALON_PROVIDER_TESTCONTAINERS=1` disposable-provider path, and the Docker-capable workflow must keep invoking the shared script instead of duplicating provider filters in YAML. Dependency-health provider-integration rows must continue failing closed when their provider name, generated row id, component doc, live runtime test, run posture, or runtime contracts drift from the source-derived provider manifest, and release-validation script tests must keep proving stale artifacts cannot omit the manifest readback.
10. Keep release validation fail-closed on machine-readable hard blockers from the generated scorecard: blocked platform gates, supply-chain blocked evidence items, public API removals, active test-coverage recommendation gaps, open test-flake quarantine entries, and invalid deployment-mode claims-report gate readback. Keep `partial`, `not-claimed`, externally gated provider lanes, SRE pending baselines, gated test-coverage recommendations, and external-policy-pending supply-chain items visible as readback/exception posture rather than accidental hard blockers.

## Refresh cadence

Refresh this page when:

- a release branch opens
- a package changes maturity or ownership
- a runtime route, snapshot key, or catalog interface changes
- a deployment-mode claim or publish-probe policy changes
- `.NET 11` or another future framework lane changes its status
- release validation adds or removes a gate
- the machine-readable scorecard schema or artifact shape changes
- a monthly architecture review closes or opens a scorecard-relevant risk

Do not append dated change logs here. The durable history belongs in commits, `ENG-*` cards, architecture reviews, and release notes.
