# CephalonTemplateApp

Generated from the Cephalon modular vertical-slice template.

## Included shape

- Blueprint: `ModularVerticalSlice`
- Project style: single ASP.NET Core host with feature slices inside a bounded module
- Starter slice: `Orders/Checkout`

## Next steps

1. Update package versions to the Cephalon feed or release you want to target.
2. Add more slices under `Modules/*/Features/`.
3. Keep endpoint, command, query, and policy code close to the feature.
4. Publish reference docs, then flip `ReferenceDocs:Enabled` to `true` when you want the host to serve them.
